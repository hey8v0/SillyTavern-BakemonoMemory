# 1.7.1 摘要来源与覆盖

## 修复范围

- 选材、队列、保存、注入、扫描计数、记忆库和摘要树共用有效来源图。失效上层释放有效下层；正常覆盖、需重建、未启用注入分别显示。
- 新保存的记录使用独立身份与内容指纹，冻结正文/标签/已保存摘要的实际输入。生成前、返回后、保存前核对；输入变化不重绑、不自动重试收费，返回文字保留为草稿。
- 仅修改标签外未消费的内容不使精确标签输入失效；实际输入、删除和回复变体变化仍受保护。旧绑定不假定当年只读取标签。
- 摘要写操作串行；保存失败撤回本次字段与来源变动，不覆盖等待期间新增的草稿。恢复包 3 保留输入快照，仍支持 1/2。
- 未改 RP 规则、模型接口或本机酒馆文件。

## 旧档操作

1. 升级前备份聊天。记录页 → 摘要来源检查 → 预览来源修复。
2. 可唯一核实的旧绑定可以迁移，应用前下载记忆恢复包。真实来源版本不匹配、缺失、同 hash 身份冲突或循环不会被强行激活。只检查/预览不调用模型。
3. 看“需重建”的最底层原因，先补课或重新生成普通摘要，再阶段总结，最后多次总结。原文确实变更不能仅靠重做最上层或改标题修复。
4. 生成范围里的“包括已覆盖材料”用于主动重整；“只使用有效材料”明确排除失效部分，覆盖范围只取实际输入。默认仍会拦住坏材料，不默默跳过。
5. 来源报告只导出标识、类型、楼层、修订、引用边与原因码；分享前仍检查隐私。恢复包含私人摘要，勿当诊断报告公开。

## 验收

基线 447 项，新增 20 项实际摘要链路回归。测试调用实际选材、生成控制器、队列、草稿服务、紧凑序列化、注入组装、楼层计数和备份；模型响应、宿主磁盘写入用可控替身。

```powershell
node --test tests/*.test.mjs
node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON tests/offline/summary-coverage-ui.mjs
```

离线 DOM 使用已有 linkedom，运行时设置 `BAKEMONO_TEST_LINKEDOM` 为其 `esm/index.js` 的 file URL，不需要在酒馆安装依赖。

本次全量 467/467 通过；137 个运行模块语法、样式表解析、离线摘要界面与差异检查通过。

覆盖：四条独立上层并存；159/160/161/200/320/400 楼；过期材料阻止 API；递归压缩回退；相同输出独立身份；标签外附注、隐藏、追加、删楼、换回复；生成中修改与切聊天；保存失败及较新草稿保留；真实紧凑序列化重载；恢复包；循环/旧身份冲突；只读修复预览与版本校验。离线 DOM 检查标签与原因、文本安全、修复前备份、诊断和循环渲染。

合成 1000 条普通摘要、1001 楼，单次离线测量：来源刷新与记录组装 588.8 ms，注入材料组装 629.1 ms，1000 条保持有效。未模拟模型 token 上限；这是电脑上的合成测量，不是手机指标。

未执行真实浏览器、手机、模型调用或反馈者故障存档测试。离线通过不等于所有宿主环境已实测；没有证据支持任意加入 160 楼上限。
