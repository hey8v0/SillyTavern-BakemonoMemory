# 剧情剪辑台开发交接摘要

更新时间：2026-07-07

## 项目位置

- 仓库源码：`C:\Users\22674\Documents\Codex\2026-05-17\bakemono-5-content-bakemono-details-summary\BakemonoMemory`
- 酒馆安装目录：`E:\SillyTavern\public\scripts\extensions\third-party\SillyTavern-BakemonoMemory`
- GitHub：`https://github.com/hey8v0/SillyTavern-BakemonoMemory`
- 当前主分支：`main`

## 插件定位

剧情剪辑台是 SillyTavern 第三方扩展，用来管理长期剧情记忆。核心目标是：

- 扫描或生成剧情摘要。
- 把普通摘要压缩为阶段总结，再把阶段总结压缩为多次总结。
- 管理补课旧正文、补写缺失摘要、表格记忆、向量召回、楼层收纳。
- 通过 `setExtensionPrompt` 把长期记忆注入上下文，尽量避免重复 token。

## 主要文件

- `manifest.json`：扩展元信息和版本号。
- `index.js`：全部主要逻辑，包括状态、扫描、生成、注入、表格、向量、自动总结、UI 事件。
- `settings.html`：插件面板 DOM。
- `style.css`：移动端抽屉、PC 工作台、手账预览、表格、向量详情等样式。

## 当前重要工作流

### 已有摘要模式

适合用户正文里已经有 `<bakemono>` 摘要块，并配合酒馆正则“五楼外只保留摘要”。

- 普通摘要通常不重复注入。
- 阶段总结、多次总结进入长期记忆注入。
- 补写缺失摘要会把生成的 `<bakemono><details><summary>剧情摘要</summary>...</details></bakemono>` 追加回原助手楼层。

### 通用旧正文补课

适合旧聊天没有摘要块。

- 旧正文补课保存到插件记忆，不写回正文。
- 补课完成后建议切回“已有摘要模式”或继续用通用模式，但要注意普通补课摘要是否注入。

### 批量摘要

统一入口，包含：

- 旧正文补课：按批生成插件内摘要。
- 补写缺失摘要：按批生成缺失楼层摘要，并可应用回原助手楼层。

## 最近改动

- 多次总结从“史诗简史”改成更通用的“长期总览 / 多次总结”。
- 向量记忆改为按楼层建立正文索引和摘要索引，支持查询重写、embedding 检索、rerank 分档、全文/摘要两档注入。
- 向量召回跳过仍在可见最近窗口内的内容，并排除 user 楼层选项。
- 表格记忆支持多套 profile、只读/可写、表格注入总开关、随正文填表解析。
- 自动总结支持自动保存、隐藏已覆盖楼层，并记录事务；但待确认页不再展示“自动总结回滚”入口，避免误触。
- 控制台的阶段总结 / 多次总结入口合并，点击后选择单次或批量。
- 生成提示词区拆成可折叠的四类：旧正文批次摘要、补写缺失摘要、阶段总结、多次总结。
- 阶段总结默认提示词新增“时间跨度”，剧情长焦要求概括章节内容时包含时间。
- 多次总结默认提示词新增“时间跨度”，时间线总览要求标注时间。
- 编辑已保存摘要标题后，显示标题优先使用用户手动标题，不再被 `<summary>` 覆盖。
- 手机端“总结”页和“生成提示词”页都有滚动稳定处理，避免 details 展开后滑不动。

## 用户明确不喜欢的设计

- 不要悬浮球，入口主要在魔法棒和可选顶部导航按钮。
- 不要危险操作无确认，删除/清空/重置等要二次确认。
- 不要按钮太多、重复入口太多；同类生成操作尽量合并。
- 不要 UI 像工具堆叠，手机端要折叠，PC 端要清楚分区。
- 不要 emoji 当主图标；UI 内尽量用 Font Awesome / 高级 icon。
- 不要摘要标题乱猜章节。补写缺失摘要应使用原楼层号，并允许 AI 根据正文写短标题。
- 不要把普通摘要和正文摘要重复注入，避免 token 浪费。
- 不要表格内容重复注入；表格上下文注入需要有总开关。

## 当前未解决或需持续观察

- 手机端不同壳层（SillyTavern / Tauri Tavern）对 `<details>`、textarea、嵌套滚动兼容性不同，展开后滑动问题需要持续实机测试。
- Qwen/Qwen3.5-27B 查询重写有时会输出英文规则残渣或只有一条线索；当前已加过滤，但 prompt 仍可能继续优化。
- 大聊天（700 楼、上万楼）性能仍要谨慎：向量索引、批量摘要、批量应用摘要都要避免一次性操作太多 DOM 或消息。
- 阶段总结、多次总结批量生成需要继续观察是否符合用户直觉，尤其是范围推导和自动隐藏联动。
- 自动总结事务记录已存在，但 UI 回滚入口隐藏；如果以后要做真正安全回滚，需要重新设计成低误触的历史记录/撤销中心。

## 新窗口继续开发注意事项

1. 先读本文件，再看 `git -C BakemonoMemory status --short --branch`。
2. 改源码后通常要同步到酒馆安装目录：
   `E:\SillyTavern\public\scripts\extensions\third-party\SillyTavern-BakemonoMemory`
3. 每次至少跑：
   - `node --check BakemonoMemory\index.js`
   - `git -C BakemonoMemory diff --check`
   - 同步后再跑安装目录的 `node --check`
4. 用户经常会自己在手机上测试浏览器效果，不一定需要 Codex 打开浏览器。
5. 推送前确认改动范围，提交信息要简短明确，推到 `main`。
6. 不要随意重构大文件；这个插件已经很大，优先小补丁、低风险改动。
