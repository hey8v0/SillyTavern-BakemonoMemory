# 剧情剪辑台开发交接文档

## 2026-08-11 / v1.3.5 / 最终模块化第三批（本地）

- 新建 `src/summary/levels.js`，集中摘要层级、下一层级和“多次总结 / 长期总览”命名。
- 新建 `src/features/summary-memory-model.js`，集中 summary/block 转换、多次总结嵌套覆盖、当前有效长期记忆、阶段覆盖集合与记忆档案记录构建；模块不接触 DOM、保存或酒馆 API。
- 新建 `src/features/summary-selectors.js`，集中普通摘要材料、正文补课材料、阶段/多次总结待处理集合和自动触发批次选择；通过 `getState` 与纯函数依赖注入，不反向导入入口。
- `index.js` 从第二批的 13,379 行降到 13,095 行。入口与新增模块语法检查及 `git diff --check` 通过；未运行测试、未打开浏览器。
- 下一批继续拆摘要目标选择对话、生成命令、草稿/任务队列与摘要 UI；完成摘要域后才进入表格域。

## 2026-08-11 / v1.3.5 / 最终模块化第二批（本地）

- 新建 `src/ui/operation-feedback.js`，统一操作反馈计时、状态文本捕获、生成流程与可见操作反馈；重要按钮捕获事件也由控制器绑定，不再使用入口全局变量。
- 新建 `src/ui/help-popover.js`，统一小 `i` 弹层定位、边缘避让、详情折叠/切页/滚动/外部点击/Esc 关闭和事件解绑。
- 新建 `src/ui/workbench-navigation.js`，接管工作台页面与菜单切换、手机折叠和滚动稳定、打开与关闭；仍由入口注入标题、扫描、完整渲染和其他 UI 控制器。
- 新建 `src/ui/workbench-layout.js`，接管设置区块归属与所有二级页返回导航；`summaryGenerationMode` 作为显式参数传入。
- 使用说明拆成 `src/features/help-guide-content.js` 与 `src/features/help-guide.js`：前者只保存文章，后者管理分类/文章状态、渲染和委托事件。
- `index.js` 从第一批检查点的 13,993 行继续降到 13,379 行。契约断言已迁到对应模块；入口与新模块语法检查、`git diff --check` 通过，未运行测试、未打开浏览器。
- 下一批开始拆摘要功能域；工作台定向刷新编排暂时保留入口，等摘要/表格/向量视图各自形成公开渲染接口后再统一抽成 app renderer，避免先制造一份几十个回调的空壳。

## 2026-08-11 / v1.3.5 / 最终模块化第一批（本地）

- 用户要求继续拆到最终结构后停下；本轮以已发布的 `fb5b876` 为完整回退点，建立注释标签 `backup/pre-modularization-v1.3.5-20260811`，并在仓库同级备份目录生成经过 `git bundle verify` 的完整历史包 `BakemonoMemory-v1.3.5-pre-final-modularization.bundle`。
- 新建 `src/features/prompt-inspector.js`：上一轮提示词清单的快照读取、分类、Token 解析、搜索、上下定位、高亮、复制和事件全部由一个依赖注入工厂管理。
- `index.js` 不再保存提示词清单的 UI 状态与缓存，也不再包含约 580 行实现和专用 jQuery 事件；只负责注入酒馆数据、Token 统计、通知、当前工作台页，并在首页概览和酒馆提示词事件中调用模块公开接口。
- `tests/index-contracts.test.mjs` 已跟随代码搬迁改为检查独立模块，仍约束搜索与导航不得保存状态或触发 `renderAll()`。
- 本批不改变配置、聊天元数据、摘要、表格、向量、注入和界面结构；遵照用户要求不运行测试、不打开浏览器，仅执行模块与入口语法检查及 `git diff --check`。
- 下一批继续抽取工作台公共 UI 基础（导航、操作反馈、帮助弹层与定向刷新装配），之后按摘要、表格、向量和其余功能域继续收缩入口，不能在本批后误判“最终模块化已完成”。

## 2026-08-11 / v1.3.5 / 提示词搜索定位

- 上一轮提示词搜索从 160ms 输入防抖改为显式执行：输入不会立刻过滤，点击放大镜或按 Enter 后才搜索；搜索按钮在输入与上次查询不同时显示强调态。
- 搜索结果按正文中的每一个命中位置建立索引，而不是只统计包含关键词的条目；结果栏显示当前来源与“第几处 / 共几处”，↑ / ↓ 可循环跳转上一处或下一处。
- 跳转时自动展开对应 Prompt 条目、突出当前命中并把它滚动到长正文视口中央；手动打开另一个匹配条目时，导航位置同步到该条目的第一处结果。
- 为避免十多万 Token Prompt 在手机端制造过量 DOM，搜索最多保留前 2,000 处导航结果并用 `+` 提示截断；单次展开最多渲染当前结果附近 240 个高亮节点，但仍保留完整正文和当前命中定位。
- 更新 README 与契约断言；遵照用户本轮要求未运行测试、未打开浏览器。发布前 `node --check index.js` 与 `git diff --check` 已通过。

## 2026-08-11 / v1.3.4 / 首页运行概览与上一轮提示词清单

- 首页从“推荐下一步与快捷操作”改为纯只读场记单，只保留运行健康、楼层覆盖、本轮注入构成和当前配置；扫描、生成、维护仍在各自页面，不再从首页跳转。
- 本轮注入不设置人为 Token 上限；总量按酒馆当前 tokenizer 统计。占比直接读取酒馆“提示词查看器”保存的上一轮完整 Prompt Token，包含角色卡、User 人设、世界书、聊天正文与其他扩展；尚无回复或注入配置已改变时显示为空，不再用部分聊天做伪实时估算。
- 注入拆为“阶段／多次总结、长期记忆、表格记忆、向量召回、模板／规则”五类，每类显示独立 Token 数和占本轮注入比例的进度条。
- 当前配置展示全局配置名、工作方式、扫描方式、生成模型、后台整理、注入深度和向量来源；首页只读，不新增配置字段或存储数据。
- 配置卡原先不直观且受通用样式挤偏的 `CFG` 缩写已替换为居中的滑杆图标。
- “本轮注入”标题右侧新增“查看上一轮”，进入只读的提示词清单页：复用酒馆提示词查看器保存的上一轮快照和实测 Token，按完整 Prompt、基础系统与预设、角色卡、User 人设、世界书、示例对话、聊天记录、扩展注入等条目展示；只有展开某一条时才把该条正文放进 DOM，并提供复制，不改写酒馆原生快照。
- OpenAI 兼容接口下不再直接显示酒馆内部用于图表的 `oaiSystemTokens`：该字段会把存在覆盖关系的系统计数合并，可能大于最终 Prompt。现在改为“基础系统与预设”，从最终 system / developer 消息中扣除已经单列的世界书、人设、角色卡、示例和扩展内容后重新计数；详情票据把模型、预设和回复楼层拆成三行完整显示，手机端不再截断。
- 提示词清单新增标题与正文全文搜索、160ms 输入防抖、清空、匹配计数、无结果状态和展开内容命中高亮；最多建立 240 个高亮节点，搜索本身不保存数据、不触发完整渲染。
- 已同步修改使用说明中依赖旧首页主按钮的文字；README 重写为正式发布手册，完整覆盖插件定位、记忆层级、七个一级入口、三类工作流、摘要 / 表格 / 向量 / 自动化 / 注入 / 主题 / 事务、API 与全局配置范围、安装更新、手机性能、安全边界和常见问题。
- 发布检查已完成：完整契约与纯模块测试 87/87 通过，`index.js` 与 `src` 下共 23 个 JavaScript 文件语法检查通过，`git diff --check` 通过；本轮未打开浏览器。

## 2026-08-11 / v1.3.3 / 常用入口、注入边界与暖纸主题

- “生成提示词”和“楼层收纳”直接进入“设置中心 → 日常使用”；生成模型页不再嵌套提示词入口，楼层收纳从安全维护底部迁到独立页面，二者都返回设置中心。
- 默认长期记忆模板在 `{{memory}}` 后新增“【剧情剪辑台：长期剧情记忆结束】”；默认注入保持开启，深度由 4 改为 999。仅识别并迁移旧内置模板，自定义模板和自定义深度不覆盖。
- 自定义主题库内置受保护的“暖纸日间”和“暖纸夜间”：都使用非纯白/纯黑的暖色纸张层次；编辑内置主题时自动另存，不能误删。升级时保留用户已有主题。
- “表格记忆注入上下文”新增独立的即时保存事件；取消勾选后立刻重建注入正文、同步所有角色卡并保持取消状态，不再因切页重新勾选。
- 设置中心“撤回与事务”顺延为第 09 项；独立楼层收纳页会直接展开收纳设置，不再额外显示一层折叠入口。
- 发布检查已完成：完整契约与纯模块测试 86/86 通过，`index.js` 与 `src` 下共 23 个 JavaScript 文件语法检查通过，`git diff --check` 通过；本轮未打开浏览器。

## 2026-08-10 / v1.3.2 / 所有角色卡共用设置

- 设置页中的工作流、扫描、生成 API、提示词、注入、自动化、表格框架和向量参数，应用或保存后会写入同一份全局活动配置；打开插件或切换角色卡时强制同步，不再要求每张卡重复保存、拉取配置。
- 从旧版本第一次升级时，会等到存在有效聊天后先用当前聊天里已经调好的设置建立共用配置，再开启强制同步；不会先拿旧的默认预设覆盖当前卡。该迁移只执行一次。
- 向量接口、密钥、模型、阈值和召回规则会跨角色卡共用；向量索引、embedding 缓存、最近召回结果、候选记录和索引时间仍按聊天独立保存，不会把某张卡的剧情记忆带到另一张卡。
- 随正文生成的运行进度、表格修改草稿、摘要、注入正文等聊天内容继续保持聊天级隔离；本次只统一配置，不改变摘要、表格行、向量记录或聊天存储格式。
- 模型拉取前会先保存刚填写的接口配置，即使请求失败或切换页面，地址和密钥也不会因重新渲染丢失；模型拉取成功后再同步模型列表与当前选择。
- 新增配置同步与运行数据隔离契约测试；发布检查需包含完整测试、全部 JavaScript 语法检查和 `git diff --check`，本轮不打开浏览器。

更新时间：2026-08-10

这份文档是给后续 Codex / 新窗口 / 压缩上下文后的自己看的项目记忆。先读它，再改插件。

## 2026-08-10 / v1.3.1 / 楼层记忆索引与基础表格

- 新建 `src/memory/floor-memory-index.js`：只在运行时从聊天楼层、已有摘要、阶段覆盖、待确认草稿、表格草稿/事务、隐藏记录和向量记录推导楼层状态；不新增聊天存储字段，不复制正文，也不加入“番外”标记。
- 新建轻量自动编排计划：新回复到达后先读取楼层状态，再协调已经开启的随正文捕获、回复后摘要/填表、阶段自动总结、自动收纳和向量刷新。各功能原有开关、阈值、模型与保存策略不变。
- 首页三项状态改为“已整理至 / 待识别楼层 / 待确认”，主卡继续只推荐一个下一步；详细原理放在小 `i` 和使用说明。“自动与数据”复用原状态票据，不新增菜单或设置页。
- 新建 `src/tables/builtin-presets.js`，把用户旧表格预设整理为可选的“基础表格”：角色特征、人物关系、世界设定、重要物品、约定、剧情指导共 6 表；去掉事件摘要、大总结及相关规则，剧情指导为只读且禁止 AI 编辑。
- 基础表格入口收在“剧情表格 → 新建与导入表格”折叠区。点击后新建独立表格组并保留当前组，不会自动覆盖用户表格；同名组已存在时只切换到现有组。旧预览版若已创建“剧情基础台账”，再次使用入口时会沿用并改成新名称。
- 设置中心“安全维护”的“撤回与事务”已并入与日常设置相同的五列网格，序号、图标、标题、状态和箭头保持同列对齐，不再使用独立卡片布局。
- 发布检查已完成：完整契约与纯模块测试 77/77 通过，`index.js` 与 `src` 下共 23 个 JavaScript 文件语法检查通过，`git diff --check` 通过；本轮未打开浏览器。

## 2026-08-10 / v1.3.0 / 局部渲染架构第三批收尾

- 定向刷新控制器新增 `SUMMARY / SCAN / ARCHIVE / INJECTION / AUTOMATION / PROMPTS / GENERATION / CONFIG / SETTINGS` 九个数据域，连同上一批的向量、待确认和表格域，覆盖全部用户业务操作。
- 扫描和摘要生成只重建当前可见的总结、记忆库、摘要树、待确认或聚合页；不再回填隐藏配置页。摘要生成的进行中、成功与失败反馈也沿用所属刷新域。
- 楼层隐藏/恢复、范围预览、自动收纳和自动事务回滚只刷新安全维护、首页隐藏统计、记忆状态或当前向量状态。
- 注入、自动总结、提示词、生成 API、工作流和整套配置分别只刷新自己的功能页及必要的聚合状态；区域配置预设不再统一调用完整渲染。
- `renderAll(` 静态出现从模块化结束时约 155 处降到 5 处：一处函数定义，四处分别用于酒馆事件合并调度、页面切换、工作台初始化和首次打开。业务函数中的完整刷新已归零。
- 契约测试已增加“完整刷新只能存在于生命周期入口”的数量边界和九个新刷新域的结构断言；发布前需要运行完整测试、入口与模块语法检查及 `git diff --check`。

## 2026-08-10 / 局部渲染架构第二批（本地，尚未发布）

- 新增 `renderWorkbenchScope()` 定向刷新控制器和 `VECTOR / DRAFTS / TABLES` 三个明确刷新域；它们只更新当前可见页面所需的面板、共享计数、顶栏状态和操作反馈，不再借道完整 `renderAll()`。
- 建立/清空向量索引、应用向量配置和测试召回，已改为只刷新向量页；若用户停留在“自动与数据”，只更新聚合状态，不重建其他隐藏页面。
- 草稿保存、丢弃、重新生成、撤回，缺失摘要补写，以及任务队列加入、重试、移除和清理，已改为只刷新待确认页；后台队列进度不会重填用户正在其他页面编辑的表单。
- 表格草稿、行列编辑、表格组/框架、导入新建、清空及撤销重做，已改为只刷新表格页；表格变化在“自动与数据”页只更新相关概览。
- `renderAll(` 静态出现由约 155 处降到 89 处，定向刷新静态出现 65 处；本批新增契约断言，防止上述高频函数退回全量刷新，但遵照用户规则没有执行测试、没有打开浏览器。
- 下一批继续处理摘要生成/扫描、楼层收纳、注入与其他预设保存等剩余 89 处；不能机械替换，需按数据依赖继续划分刷新域。

## 2026-08-10 / 局部渲染架构第一批（本地，尚未发布）

- `renderAll()` 不再无条件扫描并组装剧情摘要、阶段总结和多次总结；三层 block bundle 只在“总结”和“自动与数据”实际需要时创建。
- 酒馆实际隐藏楼层不再每次刷新都遍历，只在剪辑台首页显示该统计时读取。
- 工作流、注入、生成提示词、扫描、自动总结、批量总结和默认生成 API 的表单回填已按当前页面分流；在向量页、待确认或记忆库操作时，不再顺手覆盖其他隐藏页面的输入框。
- 这一批先减少高楼层聊天中最明显的无关扫描和跨页 DOM 写入；尚未消除全部 `renderAll()` 调用。下一批继续把保存、草稿、表格和向量操作改为各自的局部刷新目标。
- 已补入口契约测试但遵照用户规则暂不执行；未打开浏览器。

## 2026-08-10 / 混合召回 v2（本地，尚未发布）

- 新建 `src/vector/hybrid-retrieval.js`，把中文检索词拆分、稀有词权重、语义/词项候选合并和最终重排从 `index.js` 分离为纯算法模块。
- 候选不再只靠向量阈值：向量候选、中文/英文词项候选和用户重点关键词候选分别取样后合并，低相似度但命中人名、地点、物品或承诺的旧记忆仍有机会进入重排。
- 词项评分使用文档频率降低常见碎片的权重，并保留 `lexicalScore`、`matchedTerms` 和 `matchedKeywords`，召回详情可解释“为什么找到了这一条”。
- 原界面的“关键词加权”现在真正参与最终分数；旧代码写死的每次 `0.03`、最高 `0.12` 已移除，默认继续使用现有配置值 `0.18`，没有新增存储字段或迁移。
- “上次召回详情”的第二步由 `Embedding 检索` 改为“混合初筛”，显示向量相似度、词项分数、重点关键词和命中词；注入正文不额外携带这些诊断词，避免浪费 token。
- 新增 `tests/hybrid-retrieval.test.mjs` 并更新入口契约，但遵照用户规则暂不运行测试、不打开浏览器；下一阶段必须回到局部渲染架构，继续降低高楼层手机压力，不继续横向扩张记忆功能。

## 2026-08-10 / 二级页面层级返回（本地，尚未发布）

- 正式插件补齐 Demo 已有的层级返回：自动记忆、自动总结、剧情表格和向量记忆返回“自动与数据”；工作流、扫描、注入、默认生成模型、外观、整套配置和安全维护返回“设置中心”。
- 生成提示词返回“默认生成模型”，摘要树返回“总结”；主菜单一级页和使用说明不额外显示返回行。
- 返回入口复用现有页面切换机制，不新增历史状态、配置字段或存储结构；手机端保留 44px 触控高度，视觉上仍是克制的无底色文字入口。
- 本批按用户要求不自动运行测试、不打开浏览器；契约测试只同步更新，等待后续统一检查。

## 2026-08-10 / 新手导航第二阶段收尾（本地检查点，尚未发布）

- 用户确认“任务导向的场记抽屉” Demo 后，将正式插件主菜单从约 16 个平铺入口收敛为 7 个：剪辑台、总结、记忆库、待确认、自动与数据、设置中心、使用说明。
- 摘要树不再占主菜单一行，继续通过总结页已有入口进入；功能没有删除。
- 新增“自动与数据”聚合页，集中进入自动记忆、自动总结、剧情表格和向量记忆，并显示真实启用状态、表格数和向量索引数。
- 新增“设置中心”聚合页，只保留会影响多个功能的共用入口：工作流、扫描与识别、注入内容、默认生成模型、外观与主题、整套配置；撤回与事务单独放在末尾的安全维护区。
- “自动与数据”和“设置中心”的入口行已改成固定列网格；图标、标题、状态胶囊和箭头不再随状态文字宽度左右漂移，并已同步本地酒馆。
- 不再建立会和功能页重叠的“专家设置”总页。自动记忆、表格、自动总结和向量记忆的阈值、接口与模板继续留在各自页面，设置中心不复制第二份。
- 原先错放在工作流页的数据库状态、批量摘要、楼层收纳和整套配置，分别归还给“自动与数据、总结、安全维护、整套配置”；自动总结页里的共用生成 API 迁到“默认生成模型”。
- 首页重复的撤回、隐藏、恢复三组操作收成单一“安全维护”入口；生成提示词改由“默认生成模型”页继续进入，不再占设置中心一行。
- 使用说明同步写清“全局设置 / 功能内设置”的边界，并修正旧聊天补课、整套配置、酒馆主模型和向量高级参数的实际入口。
- 子页面打开时，主菜单会高亮所属聚合入口，帮助页固定在菜单底部；待确认入口显示实时数量。
- 本批只改导航信息架构、展示状态和样式，不修改配置字段、摘要逻辑、表格数据、向量数据或存储结构。
- 本批按用户要求统一运行契约测试和语法检查，同步电脑本地酒馆并建立本地 Git 检查点；不推送远端。

## 2026-08-10 / 模块化工程十四批完成（本地，尚未发布）

- 以 `1.2.6 / 36e8ad5` 为完整回退点，并建立本地分支 `backup/pre-modularization-1.2.6`。
- 新建 `src/shared/text.js`，承接哈希、列表解析、标签过滤、关键词匹配和搜索归一化等纯文本函数。
- 新建 `src/vector/math.js`，承接本地向量分词、哈希向量和余弦相似度等纯数学函数。
- `index.js` 只改为导入上述函数；没有修改聊天状态结构、配置字段、摘要流程、表格事务、注入逻辑或 UI。
- 新增 `tests/pure-modules.test.mjs` 和 `tests/parser-modules.test.mjs` 作为纯模块行为保护。
- 第二批新建 `src/tables/operation-parser.js` 与 `src/vector/query-parser.js`；表格修改语句和查询改写结果的“解析”已与真正的数据执行、保存和 UI 反馈分开。
- 第三批新建 `src/tables/rollback-plan.js` 与 `src/summary/target-selection.js`；表格级联回退边界、总结目标选择和分批规则已经与真正的状态修改、生成及保存分开。
- 新增 `tests/planning-modules.test.mjs`，并把旧契约测试的表格回退断言改为读取新模块，避免后续完整测试因函数搬家失效。
- 第四批新建 `src/summary/source-metadata.js` 与 `src/shared/prompt-utils.js`；来源楼层、排序键、提示词模板替换、旧内置提示词迁移和结构预览均已与状态及 UI 分开。
- `src/summary/target-selection.js` 现在复用统一的来源元数据模块，不再自行维护第二套楼层排序逻辑；新增 `tests/prompt-metadata-modules.test.mjs`。
- 第五批新建 `src/tables/schema-utils.js` 与 `src/shared/injection-template.js`；多种表格 JSON 的归一化、schema 匹配、旧行保留、注入模板剥离与套壳均已变成无副作用模块。
- 第六批新建 `src/vector/provider-config.js` 与 `src/summary/draft-parser.js`；OpenAI 兼容接口地址和模型列表解析、补课摘要草稿解析已经与请求、状态保存和 UI 分开。端点构造刻意保持旧行为，避免无意改变 Termux 与自定义代理兼容性。
- 新增 `tests/schema-injection-modules.test.mjs` 与 `tests/provider-draft-modules.test.mjs`。用户同意采用“每个模块批次跑少量定向测试，最后只跑一次完整测试”的节省 token 策略。第一阶段完整契约检查点已运行，现有 7 个测试文件合计 41/41 通过。
- 第七批开始进入状态服务层，新建 `src/core/config-sync.js`，承接全局配置读取、版本签名、聊天配置同步判定和已应用标记；实际读取酒馆存储、套用配置和保存仍由 `index.js` 编排。新增 `tests/state-config-sync.test.mjs`，新模块 3/3、与入口契约联合定向测试 26/26 通过。
- 第八批新建 `src/vector/storage.js` 与 `src/core/persistence.js`；embedding 压缩、向量文本裁剪、保存前缓存/记录瘦身和 debounced 保存调用次序已从入口分离。`saveState()` 仍在每次调用时读取当前 `chat_metadata`，不会缓存聊天状态引用。
- 新增 `tests/persistence-modules.test.mjs`，新模块 4/4；第七、八批状态模块与入口契约联合定向测试 31/31 通过，入口和两个新模块语法检查通过。
- 第九批新建 `src/core/chat-switch.js`，聊天切换的配置同步、自动收纳、向量失效、注入刷新和延迟渲染顺序已集中；`CHAT_CHANGED` 事件注册仍留在入口，原因字符串与 fail-fast 行为保持不变。
- 新增 `tests/chat-switch-flow.test.mjs`，协调器 3/3；第七至九批状态模块与入口契约联合定向测试 35/35 通过，入口和新模块语法检查通过。
- 第十批新建 `src/core/state-shape.js`，统一只补 `undefined` 的深拷贝默认值和数组字段形状修复；`ensureState()` 中直接数组修复已归零，机械的默认值循环只剩按生成目标类型遍历的一处。
- 新增 `tests/state-shape.test.mjs`，工具 2/2；第七至十批状态模块与入口契约联合定向测试 37/37 通过，入口和新模块语法检查通过。
- 第十一批在 `src/core/state-shape.js` 增加嵌套对象容器补全，替换自动收纳、自动化、生成目标、正文后处理、表格、向量和聊天保护状态中的重复三元表达式；为兼容旧存档，数组仍按既有规则视作对象。
- 状态形状工具 3/3；第七至十一批状态模块与入口契约联合定向测试 38/38 通过。
- 第十二批新建 `src/core/workflow-mode.js`，集中四套枚举和 `memoryStrategy`、`workflowMode`、`stageSourceMode`、`outputMode` 的关联回退顺序；有效下游选择继续保留，不会因其他无效字段被整套重置。
- 工作方式策略 3/3；第七至十二批状态模块与入口契约联合定向测试 41/41 通过。
- 第十三批新建 `src/core/prompt-migrations.js`，集中旧补课/通用提示词、阶段与多次总结时间轴、正文后处理、向量查询改写和全局预设的迁移编排；默认提示词与用户自定义文本的保留语义未改。迁移编排 4/4，提示词相关联合定向测试 34/34 通过。
- 第十四批不再拆代码，完整运行 13 个测试文件共 64/64 通过；`index.js` 与 19 个模块合计 20 个 JavaScript 文件语法检查通过，`git diff --check` 无内容错误（只有 LF/CRLF 提示）。
- 当前 `index.js` 为 12,824 行，形成 19 个职责模块；`renderAll(` 静态出现 155 处。SillyTavern API、DOM/事件、异步业务编排、表格 profile 合并和聊天引用清理继续留在入口，不能再按纯搬家方式安全拆分。
- 本轮纯工程模块化正式结束。下一阶段转向降低学习成本的新手引导与工作流首页；不要继续为了减少行数而拆入口。
- 这些改动尚未同步到本地酒馆、尚未提交、尚未推送，也没有运行浏览器验证。

## 2026-07-25 / v1.2.6

- 修复向量模型拉取开始时整页重绘，导致尚未保存的嵌入来源、接口地址、密钥和模型被旧聊天状态覆盖的问题。
- 拉取嵌入模型或查询改写模型前会先快照并保存当前向量表单；请求失败后配置仍然保留，不会再陷入反复填写接口地址的循环。
- 两种模型拉取现在都会明确返回成功或失败，避免请求失败时外层操作反馈误报“模型列表已更新”。

## 2026-07-15 / v1.2.5

- 自定义主题升级为全局主题配置库：可选择、使用、覆盖、另存和删除多套主题配置。
- JSON 支持导出当前主题、导出全部主题配置包，以及从文本或文件导入单套/整包主题。
- 手机主题编辑页改成“色板 / 质感 / JSON”三个互斥短分页；一次只显示一类编辑器，避免长距离滚动。
- 主题来源、配置库和编辑分页仍只写入全局外观设置，不进入聊天配置或记忆数据。

## 2026-07-15 / v1.2.4

- 新增全局“自定义主题”入口；默认仍完全跟随酒馆主题，不改现有配色。
- 自定义主题以 `bakemono-memory-theme/v1` JSON 导入、导出和复制，只接受白名单颜色与数值令牌，不执行 CSS 或脚本，也不写入聊天记忆数据。
- 当前全局整套配置现在带版本签名；初始化及切换聊天时，旧聊天会自动接收最新全局配置一次，随后继续保留该聊天内的临时修改。
- “酒馆主模型”仍走 SillyTavern `generateRaw`；跨聊天自动同步会同时带回整套配置中的 API 来源，不再需要手动重新拉取配置。

## 一句话定位

剧情剪辑台是一个 SillyTavern 第三方扩展，用来把长篇 RP 的正文、摘要、阶段总结、多次总结、表格记忆、向量召回和楼层收纳整理成可长期使用的上下文记忆。

用户的核心诉求不是“做一个摘要按钮”，而是：

- 能在几百楼、几千楼甚至上万楼聊天里持续记住剧情。
- 能兼容用户已有的 `<bakemono>` 正文摘要正则工作流。
- 能在没有摘要的旧聊天里补课。
- 能把缺失的正文摘要补回原助手楼层，继续走用户原本的摘要流。
- 能用表格记录角色、关系、事件、设定、物品、约定、剧情指导等结构化信息。
- 能用向量召回被隐藏或被压缩掉的旧剧情，但不要爆 token。
- 手机端必须好用，因为用户主要用手机酒馆。

## 路径与仓库

- 工作区根目录：
  `C:\Users\22674\Documents\Codex\2026-05-17\bakemono-5-content-bakemono-details-summary`
- 插件源码目录：
  `C:\Users\22674\Documents\Codex\2026-05-17\bakemono-5-content-bakemono-details-summary\BakemonoMemory`
- 酒馆实际安装目录：
  `E:\SillyTavern\public\scripts\extensions\third-party\SillyTavern-BakemonoMemory`
- GitHub：
  `https://github.com/hey8v0/SillyTavern-BakemonoMemory`
- 主分支：
  `main`

用户经常说“推 github / 推一下”，通常表示：

1. 修改 `BakemonoMemory` 源码。
2. 同步到 `E:\SillyTavern\public\scripts\extensions\third-party\SillyTavern-BakemonoMemory`。
3. 跑静态检查。
4. 提交并推送到 GitHub `main`。

但如果用户没有明确要求推送，只改源码即可，最终说明“还没推”。

## 常用命令

检查仓库状态：

```powershell
git -C BakemonoMemory status --short --branch
```

检查 JS：

```powershell
node --check BakemonoMemory\index.js
```

检查 diff 空白问题：

```powershell
git -C BakemonoMemory diff --check
```

同步到酒馆安装目录：

```powershell
$src='C:\Users\22674\Documents\Codex\2026-05-17\bakemono-5-content-bakemono-details-summary\BakemonoMemory'
$dst='E:\SillyTavern\public\scripts\extensions\third-party\SillyTavern-BakemonoMemory'
foreach ($name in @('manifest.json','index.js','settings.html','style.css','DEVELOPMENT_HANDOFF.md')) {
  Copy-Item -LiteralPath (Join-Path $src $name) -Destination (Join-Path $dst $name) -Force
}
```

检查安装目录 JS：

```powershell
node --check E:\SillyTavern\public\scripts\extensions\third-party\SillyTavern-BakemonoMemory\index.js
```

提交推送：

```powershell
git -C BakemonoMemory add manifest.json index.js settings.html style.css DEVELOPMENT_HANDOFF.md
git -C BakemonoMemory commit -m "简短英文提交信息"
git -C BakemonoMemory push origin main
```

注意：`E:\SillyTavern...` 在沙盒外，复制过去通常需要提权。

## 文件结构

`manifest.json`

- SillyTavern 扩展元信息。
- 版本号在这里，用户问过为什么别人是 `0.1/0.2` 而这里一直 `1.0.0`，后来开始手动 bump。

`settings.html`

- 扩展设置面板的 DOM 模板。
- 现在 UI 比较大，很多面板都在这里。

`style.css`

- 手机抽屉、底部导航、PC 工作台、手账预览、表格、向量详情、批量摘要弹窗、内部通知等样式。
- 手机端 `<details>`、textarea、嵌套滚动问题很多，改动要保守。

`index.js`

- 主要逻辑全部在这里，是一个很大的单文件。
- 包含状态默认值、迁移、扫描、分类、摘要生成、批量任务、自动总结、楼层隐藏、注入、表格、向量、UI 渲染、事件绑定。
- 尽量不要做大重构。用户更需要稳定迭代。

`DEVELOPMENT_HANDOFF.md`

- 本文件。用于新窗口接续开发。

## 核心状态与存储

当前聊天级数据主要存在 `chat_metadata` 中的插件 key 里。

全局配置 / profile / 预设主要存在 `extension_settings` 中的插件 key 里。

重要状态概念：

- `blocks`：扫描出来的摘要或文本片段。
- `storySummaries`：普通剧情摘要 / 正文摘要。
- `stageSummaries`：阶段总结。
- `epicSummaries`：过去叫“史诗简史”，现在更倾向叫“多次总结 / 长期总览”。
- `generationPrompts`：生成提示词，包括旧正文批次摘要、补写缺失摘要、阶段总结、多次总结等。
- `scanRules`：扫描标签、排除标签、全文管线 / 标签块模式等。
- `classificationRules`：普通摘要、阶段总结、多次总结的分类关键词。
- `previewLayouts`：手账预览分段规则。
- `taskQueue`：批量摘要、补课、缺失摘要、自动处理等任务队列。
- `drafts`：待确认草稿。
- `history`：保存记录。
- `tableDatabase` / table profiles：表格框架、表格组、行数据、表格 prompt。
- `inlineGeneration`：随正文生成摘要 / 表格相关配置。
- `vectorMemory`：向量索引、召回参数、模型配置、上次召回详情。
- `autoHideRecent`：楼层自动收纳 / 只保留最近 N 楼正文。
- `autoSummaryTransactions`：自动总结事务记录，用于以后做安全回滚；当前 UI 里不展示回滚按钮，避免误触。

## 三套主要记忆工作流

### 1. 已有摘要模式 / Bakemono 摘要流

适用场景：

- 用户正文里本来就让模型输出 `<bakemono>` 摘要。
- 用户的酒馆正则会让 5 楼外只保留摘要，减少 token。
- 用户希望插件读取这些摘要，再做阶段总结 / 多次总结。

关键原则：

- 普通剧情摘要通常不重复注入，因为正文正则已经会发送摘要。
- 阶段总结和多次总结要注入长期记忆。
- 如果模型某一楼偷懒没写摘要，用“补写缺失摘要”，把摘要补回原助手楼层正文末尾。
- 补写缺失摘要输出必须是：

```html
<bakemono>
<details>
<summary>剧情摘要</summary>
...
</details>
</bakemono>
```

标题要求：

- 不要猜章节号。
- 必须保留原楼层数字。
- 允许 AI 根据正文写短标题。
- 推荐形态类似：
  `第123楼：短标题`

不要写成：

- `第x章`
- `第123楼：正文摘要` 这种死标题，用户不喜欢。

### 2. 通用旧正文补课

适用场景：

- 老聊天没有 `<bakemono>` 摘要。
- 用户中途才开始使用插件。
- 需要把 700 楼甚至更多旧正文批量压缩成插件内摘要。

关键原则：

- 旧正文补课保存到插件记忆，不写回正文。
- 它和“补写缺失摘要”不是一回事。
- 补课完成后，应提示用户切回“已有摘要模式”继续 RP，或者继续使用通用模式。
- 用户曾经困惑：“补课完了之后要怎么办？”所以 UI 文案要明确：
  旧正文补课是补过去；之后继续 RP 最好让正文恢复正常摘要流。

### 3. 补写缺失摘要

适用场景：

- 用户正在用已有摘要模式。
- 某些助手楼层缺了 `<bakemono>`。
- 用户希望补回来，继续被正则和插件扫描。

关键原则：

- 默认只处理助手楼层，不处理 user 楼层。
- 自动跳过已经含摘要标签的楼层。
- 不应该一楼一个 API 调用。大批量时必须按批处理。
- 应用草稿时会修改原助手楼层，把摘要追加到正文末尾。
- 应用很多草稿时必须分批、让 UI 有响应，避免手机卡死。

用户曾经一次扫出 360 个缺失摘要，不能生成 360 个任务逐个请求 API；要按批次生成。

## 阶段总结与多次总结

阶段总结：

- 输入普通剧情摘要。
- 输出一个更高层级的阶段总结。
- 默认提示词已经要求：
  - 标题行包含“时间跨度”。
  - 剧情长焦概括章节内容时包含时间。

多次总结：

- 过去叫“史诗简史”，用户觉得太宏大，尤其日常 RP 不合适。
- 现在统一倾向叫“多次总结 / 长期总览”。
- 用来继续压缩阶段总结，避免阶段总结无限堆积。
- 默认提示词已经改成：
  - `<summary>【多次总结·长期总览】</summary>` 或类似长期总览措辞。
  - 标题行包含“总跨度”和“时间跨度”。
  - 时间线总览按时间顺序整理并标注时间。

用户反馈过：

- 多次总结只注入一条的问题，要确认不会只取最新一条。
- 阶段总结编辑后标题被 `<summary>` 覆盖的问题，已改为手动标题优先。
- 阶段总结 / 多次总结按钮太多，所以控制台里单次和批量入口合并：点击后选择单次或批量。

## 注入逻辑

注入内容进入 `setExtensionPrompt`。

顺序原则：

1. 多次总结 / 长期总览。
2. 未被多次总结覆盖的阶段总结。
3. 是否注入普通摘要取决于模式。
4. 表格记忆按总开关和表格规则决定。
5. 向量召回作为动态补充。

已有摘要模式：

- 普通剧情摘要不重复注入，避免和正文正则重复。
- 阶段总结、多次总结应注入。

通用旧正文补课模式：

- 普通补课摘要可以注入，否则隐藏旧楼后会失忆。
- 被阶段总结覆盖后，普通补课摘要应退出注入。

表格：

- 用户要求“表格内容注入上下文”有总开关。
- 表格内容不要同时在长期记忆注入和随正文填表 prompt 中重复塞两遍。
- 随正文填表需要携带当前表格数据，但要注意 token。

向量：

- 不应召回已经在长期记忆里注入的阶段总结 / 多次总结，避免重复。
- 默认不索引或不召回 user 楼层，因为用户认为 user 楼层通常意义不大。

## 向量记忆设计

目标：

- 让被隐藏、被压缩、离当前窗口很远的旧剧情可以按需召回。
- 能在上万楼聊天中继续工作。
- 避免召回太多造成 token 爆炸。

当前概念：

- 建索引：对每楼正文 / 摘要建立记录。
- 查询重写：把当前输入和最近剧情改写成多个检索线索。
- 混合初筛：分别取得向量候选、词项候选和重点关键词候选，再合并排序。
- Rerank 分档：决定哪些发全文、哪些发摘要。
- 最终注入：按预算输出到上下文。

用户喜欢的召回详情布局：

- 上次召回详情。
- 查询重写：显示“检索意图 + 多条线索”。
- 混合初筛：显示相似度、词项分数、重点关键词和命中词的候选列表。
- Rerank 分档：全文档 / 摘要档。
- 最终注入：最终进入上下文的内容。

注意：

- 用户明确说不要照抄别人的插件 UI / 文案。
- 可以学习逻辑，但外观和措辞要有自己的风格。
- 当前“查询重写”曾出现问题：
  - Qwen/Qwen3.5-27B 输出英文规则残渣。
  - 输出 `Clue 1`、`Analyze the Request`、`Current context` 之类提示词内容。
  - 只输出一条线索。
- 已经做过过滤，但仍要观察。

推荐查询重写输出结构：

```text
意图：一句中文，概括当前需要找什么旧记忆
线索1：具体旧剧情检索问题
线索2：具体旧剧情检索问题
线索3：具体旧剧情检索问题
线索4：具体旧剧情检索问题
线索5：具体旧剧情检索问题
```

不要让模型输出：

- 英文系统规则。
- 分析过程。
- “Thinking Process”。
- “Analyze the Request”。
- 当前 prompt 原文。

向量参数用户经常问含义，应在 UI 文案里简明解释：

- 重排候选数：embedding 先取多少条进入 rerank。
- 向量相似阈值：低于这个连摘要档都不要。
- 重排全文阈值：高于这个可发全文。
- 召回全文数：最多几条全文。
- 最终召回条数：全文 + 摘要总数。
- 起召 AI 楼数：聊天太短时不召回。
- 可见最近窗口楼数：仍在可见上下文里的内容不召回。

用户困惑过“最近窗口”：

- 它不是酒馆实际渲染条数。
- 更合理的逻辑应该考虑隐藏楼层和实际可见 / 已发送内容。
- 如果楼层已隐藏，就不该因为楼号在最近 N 内直接跳过。

## 表格记忆

目标：

- 支持用户自定义多张表。
- 记录结构化信息：角色特征、人物关系、事件摘要、世界设定、重要物品、约定、大总结、剧情指导等。
- 表格内容可以注入上下文。
- AI 可以随正文输出 `<tableEdit>`，插件解析并更新表格。

重要规则：

- 只读表：适合“剧情指导”，用户手写，只读，禁止 AI 修改。
- 可写表：AI 可以插入 / 修改 / 删除。
- 表格 profile 分全局 / 角色 / 当前聊天。
- 切换 profile 要提示当前数据不会丢，只是切换本聊天使用的表格组。

表格操作格式：

```text
insertRow(tableIndex, {"0":"值"})
updateRow(tableIndex, rowIndex, {"列号":"新值"})
deleteRow(tableIndex, rowIndex)
```

注意：

- 曾经单引号导致解析失败，例如字符串里出现 `'有趣'`。
- 解析器最好宽容，但 prompt 也要要求字符串里不要出现双引号和奇怪引号。
- 随正文填表曾经重复读取两遍，导致重复行。
- 删除聊天楼层后表格不会自动回退，这是一个设计难点。
- 用户曾经提出“表格跟随聊天记录走”，但这会和插件 DB 模式冲突，改动要非常谨慎。

UI 偏好：

- 表格内容要在表格页首屏。
- 表格框架、表格组、导入、提示词等配置应折叠或放后面。
- 点开表格编辑区后，要和原始预览表格有明显视觉区别。
- 删除行要二次确认，或有撤销重做。

## 楼层收纳 / 隐藏楼层

用户想要类似隐藏助手的“自动只保留最近 N 楼正文”。

核心诉求：

- 不是点一次隐藏一次。
- 开启后每次新消息 / 删除 / reroll 后都应动态整理。
- 删除最新楼层时，之前自动隐藏的楼层应按规则恢复。
- UI 不要太复杂。

用户要求楼层收纳 UI 保留三个核心：

1. 启动自动收纳开关。
2. 只保留最近正文楼数。
3. 恢复自动收纳的楼。

其他如“应用自动收纳”“立即整理一次”不需要长期露出；开启时可以自动执行一次。

注意：

- 自定义楼层范围隐藏 / 恢复仍有用，可以保留。
- “高级”两个字用户不喜欢，楼层收纳是常用功能。
- 控制台隐藏楼层数字最好能读取酒馆实际隐藏楼层，而不是只显示插件管理的数量。

## 自动总结

功能：

- 自动检测未总结摘要数量。
- 自动生成阶段总结草稿或自动保存。
- 可自动隐藏已覆盖楼层，保留最近 N 楼上下文。

风险：

- 用户曾经遇到：
  1. roll 了两层。
  2. 自动总结刚好触发。
  3. 自动隐藏 50-96 楼。
  4. 用户想删除 96 楼重 roll，担心状态不会回退。

后来方向：

- 增加 `autoSummaryTransactions` 事务记录。
- 记录自动总结覆盖范围、生成的总结、隐藏范围。
- UI 里不要放显眼“自动总结回滚”，因为用户觉得待确认里的回滚容易误触。
- 如果未来做回滚，应做成低误触的历史 / 撤销中心。

## UI / UX 原则

用户明确偏好：

- 手机端优先。用户主要在手机酒馆测试。
- PC 端也要清楚，但不要为了 PC 大改把手机弄坏。
- 不要悬浮球。
- 入口：
  - 魔法棒菜单。
  - 可选顶部导航栏按钮。
  - 扩展设置里有开关控制是否显示顶部按钮。
- 颜色跟随 SillyTavern 主题。
- UI 要有手账感，但不要乱、不要堆。
- 尽量使用 Font Awesome / 高级图标，不要用 emoji 当 UI 主图标。
- 危险操作必须二次确认。
- 重要按钮点击后要有反馈：toast、内部通知、loading、禁用态、进度。
- 不要外部浏览器 alert 风格，尽量用插件内部提示框。
- 长表单在手机端默认折叠。
- 相同功能不要出现两套入口。
- 标题随当前模块变化，比如“表格”“向量记忆”“总结”。

用户不喜欢：

- “工具面板感”。
- 入口藏太深。
- 按钮太多。
- 同一个设置在多个地方重复。
- 保存按钮其实是另存为，导致一堆同名配置。
- 载入按钮多余：选择配置后应直接切换，必要时再确认使用。
- 史诗简史这个名字过于宏大。
- 小 i 提示一直露在标题上；折叠时应隐藏，展开后再出现。
- 小 i 的外圈和阴影太突出。
- UI 动效导致闪屏。

## 手机端滚动与闪屏问题

这是长期重点。

已出现过的问题：

- 总结页点开一个摘要能滑，关掉再点另一个摘要就滑不动。
- 摘要少的时候更容易复现，摘要多的时候反而不明显。
- SillyTavern 和 Tauri Tavern 都能复现。
- 生成提示词编辑页也出现过类似问题。
- 点开记忆库 / 摘要详情会闪屏。

可能原因：

- `<details>` 展开 / 收起触发高度重排。
- 父容器和子容器都有 `overflow`。
- 移动端 WebView 对嵌套滚动和 textarea 很敏感。
- CSS 动画 / transform / backdrop-filter 可能加重闪屏。

已有缓解：

- `stabilizeMobileWorkbenchScroll(expectedTab)`。
- 展开 details 后延迟修复滚动容器高度和 overflow。
- 减少部分动效。

以后改 UI 时要注意：

- 不要给大滚动容器随便加 transform。
- 不要让很多卡片同时动画。
- details 展开后要确保外层仍能滚动。
- textarea 高度要有限制，内部可滚动。
- 手机端改动必须让用户实机验证。

## 批量任务与性能

用户的典型规模：

- 700 多楼聊天。
- 360 个缺失摘要。
- 未来目标上万楼。

性能要求：

- 不要一楼一个 API 调用。
- 不要一次性渲染几百张大卡。
- 不要一次性修改几百条楼层正文导致酒馆卡死。
- 批量应用应分批、可取消、可清理卡住任务。
- 任务队列里生成中的旧任务必须能解除卡住。

用户遇到过：

- 360 个补写缺失摘要生成 360 个任务。
- 一键应用 17 个摘要就让酒馆卡住。
- 旧的“旧正文补课 14/15”任务卡在队列里，取消不了。

后续如果改任务队列：

- 要有“清理卡住任务”。
- 运行中任务也应有超时 / 强制移除。
- 批量摘要任务应按批记录，而不是每楼一个任务。
- UI 应清楚显示正在做什么、进度多少、能否取消。

## 提示词体系

现在生成提示词区应该至少包含：

- 旧正文批次摘要提示词。
- 补写缺失摘要提示词。
- 阶段总结提示词。
- 多次总结提示词。
- 自动记忆正文摘要提示词。
- 表格修改提示词。
- 随正文摘要提示词。
- 随正文表格提示词。
- 查询重写提示词。

用户希望：

- 都能自定义。
- 能保存预设。
- 能切换预设。
- 手机端不要一口气展开所有文本框。

注意区分：

旧正文批次摘要：

- 用于没有摘要的旧正文。
- 结果进入插件记忆。
- 不写回正文。

补写缺失摘要：

- 用于已有摘要模式里漏掉摘要的助手楼层。
- 结果追加回原助手楼层正文。
- 必须输出 `<bakemono><details><summary>剧情摘要</summary>...`。

自动记忆正文摘要：

- 回复后处理。
- 如果保存到插件草稿，可用 `<summaryDraft>`。
- 如果要写回正文，就必须改成 `<bakemono>`。
- 当前用户问过这点，要保持文案明确。

## 用户个人工作流记忆

用户常用方式：

1. 让正文模型每楼输出 `<bakemono>` 摘要。
2. 酒馆正则负责 5 楼外只保留摘要。
3. 插件扫描摘要。
4. 累积到一定数量后生成阶段总结。
5. 隐藏被覆盖楼层。
6. 阶段总结多了后生成多次总结。
7. 向量召回补足旧剧情细节。
8. 表格记录结构化信息。

用户也会：

- 中途打开旧档补课。
- reroll / 删除楼层。
- 手机端频繁测试。
- 自己推 GitHub 或让 Codex 推。
- 在不同聊天之间切换，希望配置全局保留。

## 明确不要做的事

- 不要 `git reset --hard`。
- 不要回退用户没让回退的改动。
- 不要一次性重写整个插件。
- 不要强行打开浏览器测试，用户经常说酒馆她自己看。
- 不要把别人插件的文案 / UI 直接抄过来。
- 不要把危险操作做成一键无确认。
- 不要在移动端放太多常驻大按钮。
- 不要把摘要标题变回固定 `剧集终了·点击回看`。
- 不要让普通摘要在已有摘要模式里重复注入。
- 不要让表格内容在两个注入位置重复出现。

## 2026-07-10：提示词、批量渲染与表格事务回退

- 阶段总结的“剧情长焦”改为按事件输出章节跨度、时间、地点、在场角色、经过和关键点；“角色进化录”与“金句名人堂”改为开放数量的示例格式。
- 多次总结的“时间线总览”使用同一套带时间轴的剧集目录体格式。
- 所有结构化 Bakemono 默认提示词中，除第四面墙外，每个 `➤` 段落末尾增加独立一行 `……`，提示模型示例不是数量上限。
- 内置默认预设和仍在使用旧版内置文案的聊天状态会迁移到新提示词；用户完全自定义的提示词不强制覆盖。
- 批量任务队列处理中不再每个状态变化都全量 `renderAll()`，只局部刷新草稿与队列；聊天消息事件改为按动画帧合并刷新。
- 表格自动修改支持按来源楼层查找非栈顶事务。删除、编辑或重 roll 较早楼层时，会恢复到最早受影响事务之前，并级联撤销依赖它的后续表格事务，避免表格留在不可能的状态。
- 表格级联回退会写入 `tableDatabase.rollbackHistory`，最多保留 20 条记录。
- 新增 `tests/index-contracts.test.mjs`，覆盖提示词版式、续写省略号、非栈顶表格事务回退计划和高频渲染边界。

## 2026-07-10：手机高楼层内存降载

- 用户反馈手机 Termux / 酒馆在几百楼聊天中会突然回到刚启动状态，表现更像 Android 内存压力导致页面或 Node 进程被回收，而不是普通 JS 异常。
- 根因审查发现 `ensureState()` 曾在几乎每次状态读取时重复清洗全部聊天状态并重建 `memoryRecords`；现在聊天长度减少时才重新清理越界引用，派生记忆记录只在需要显示的页面生成。
- 工作台关闭时，`renderAll()` 与后台任务进度不再创建隐藏页面 DOM；工作台打开后只渲染当前标签页，切换标签页时再按需生成该页内容。
- `openWorkbench()` 从“扫描时完整渲染一次 + 打开后再完整渲染一次”改为“后台扫描 + 单次渲染”。所有 `persist: false` 的后台扫描默认不触发额外全量渲染。
- 扫描后旧块匹配由逐项 `find()` 的 O(n²) 路径改为 Map 索引；扫描预览内存最多保留 240 条，手机最多创建最近 60 条预览 DOM，完整命中数量仍会显示。
- `1.1.28` 曾尝试把 `memoryRecords` 与 `scanPreview` 改成非枚举字段，并对整个聊天状态做 WeakSet 早退缓存；手机端出现配置区无法初始化、无法保存和无法点击的严重兼容回归。
- `1.1.29` 已撤销上述两项高风险状态结构改造，恢复普通可枚举数组和每次配置补全；保留隐藏工作台不渲染、活动标签页按需渲染、线性扫描、预览限量和时间线分页延迟建 DOM 等不改变配置结构的优化。
- 契约测试增加酒馆元数据对象兼容边界，禁止再次在 `ensureState()` 中使用属性描述符或整状态早退缓存。
- `1.1.30` 修复按标签页渲染后的控件归属错误：整套配置下拉框实际位于“控制台/overview”，却只在“生成提示词/prompts”页刷新，导致下拉框偶尔有值、重新打开又为空。现在 overview 每次渲染都会单独刷新整套配置选择器，不需要渲染其他隐藏配置页。
- `1.1.31` 将同一修复扩展到全部分区配置：扫描页刷新扫描预设，自动页刷新自动总结和 API 预设，自动记忆/表格页刷新正文摘要预设，向量页刷新向量预设，注入页刷新注入预设，提示词页刷新提示词预设。新增完整映射契约，防止懒渲染再次漏掉某个配置界面。
- `1.1.32` 将侧边菜单顶部品牌徽标里的“剪”字替换为与酒馆插件入口一致的 `fa-clapperboard` 场记板图标；保留原有主题色、尺寸和布局，并补充静态契约防止图标回退。

## GitHub 提交与更新日志索引

说明：
- 本地可以精确查到每个 commit 的时间、标题和改动文件。
- “每次 push 到 GitHub 的批次”不一定能从本地还原，因为一次 push 可能包含多个 commit。
- 因此这里按 commit 和日期整理成更新日志索引，方便新窗口快速理解项目演化。

截至 2026-07-07，`main` / `origin/main` 已有 68 个提交。最近一次远端提交为：

- `3b58735` / 2026-07-07 / `Fix prompt editor mobile scroll`

### 2026-05-17：插件从零搭建

- `793625b Initial Bakemono Memory extension`
  - 新增插件基本结构：`manifest.json`、`index.js`、`settings.html`、`style.css`、`README.md`。
  - 初版目标是扫描 `<bakemono>` 摘要块、生成更高层级总结、保存到聊天元数据并注入上下文。
  - 初版代码量很大，基本奠定“剧情剪辑台”的核心功能。
- `4c85ebc Initial commit`
  - 补了远端仓库初始 README。
- `19f217a Merge remote main`
  - 合并远端 main。
- `570dfba Fix settings.html path`
  - 修复 SillyTavern 加载 `settings.html` 的路径问题。
- `8410aab Improve backfill ordering and presets`
  - 改进旧正文补课 / backfill 的排序。
  - 添加或调整预设逻辑。
- `0d9ee51 Refresh scanned blocks and enrich generic prompts`
  - 扫描规则切换后刷新已扫描片段，避免旧扫描结果残留。
  - 扩充通用提示词，让没有 `<bakemono>` 的正文也能压缩。

### 2026-05-18：工作流、生成范围和手机 UI 初步成型

- `9519a08 Add generic backfill workflow`
  - 新增通用旧正文补课工作流。
  - 加入对应 UI 和样式。
- `981fa2b Simplify workflow overview`
  - 简化总览页工作流说明。
  - 让新用户更容易理解当前模式。
- `5633cef Add target selection and auto commit hide mode`
  - 生成阶段总结时加入目标选择。
  - 加入自动保存并隐藏已覆盖楼层的模式。
- `dd17db4 Confirm generation ranges and configurable auto hide`
  - 生成总结前弹出范围确认。
  - 自动隐藏保留楼层数变成可配置。
- `c148c65 Use custom range dialog for generation`
  - 把生成范围从固定面板迁移到弹窗。
  - 改进移动端范围选择体验。
- `270b480 Improve target dialog and mobile nav state`
  - 修复范围弹窗位置和底部导航选中态。
- `4c35a99 Refine mobile dialog and nav active state`
  - 继续微调手机端弹窗和导航状态。
- `bcf3fdb Simplify range controls and add mobile folds`
  - 移除总览固定“生成范围”区域。
  - 手机端加入折叠布局，长表单默认收起。

### 2026-05-19 至 2026-05-20：配置预设和自定义 API

- `57e65a5 Split config presets by section`
  - 配置预设拆分为不同区域保存。
  - 解决切换工作流导致扫描过滤、自动总结等配置被重置的问题。
- `462ac0d Add custom API model presets`
  - 添加自定义 OpenAI-compatible API 的模型配置预设。
  - 包括模型名、接口地址、密钥、流式选项等。
  - 为“不用酒馆主 API”的用户提供基础配置保存。

### 2026-06-25：向量记忆和表格数据库大功能加入

- `7c30737 add vector memory and turn context options`
  - 新增向量记忆：embedding、召回、最近上下文选项。
  - 新增回复后处理时携带角色卡 / 世界书等上下文选项。
  - 这是插件从“摘要总结器”变成“长期记忆系统”的关键提交。
- `a9199a9 add table schema scopes`
  - 表格框架支持全局 / 角色 / 当前聊天作用域。
  - 解决切换聊天后表格框架丢失的问题。
- `1130082 expand table inline and vector memory flows`
  - 扩展随正文输出表格、表格自动处理和向量记忆流程。
  - 引入 `<tableEdit>` 等解析流程。
- `c3e4ba7 Fix table row add interaction`
  - 修复数据行“新增一行”无效或折叠面板自动收起的问题。

### 2026-06-26：表格编辑修复、导航改造、多次总结概念出现

- `e443623 Fix table editing and vector model controls`
  - 修复表格编辑和向量模型控制项。
  - 涉及拉取模型、保存配置等交互。
- `d10e429 Fix table field editing persistence`
  - 修复字段提示词新增 / 删除字段不生效的问题。
- `c32e493 Refine workbench navigation and table auto apply`
  - 重构左侧模块导航。
  - 表格修改草稿支持自动应用。
  - 移除部分重复设置入口。
- `ffee200 Polish mobile hint popovers`
  - 调整手机端小 i 提示气泡。
  - 目标是折叠时隐藏、展开时显示，减少遮挡。
- `29d9e28 Generalize epic summaries into multi summaries`
  - 把“史诗简史”泛化为“多次总结 / 长期总览”。
  - 解决日常 RP 中“史诗”命名太宏大的问题。

### 2026-06-27：配置逻辑、表格安全、表格 UI 和去重

- `a7c8f5d Simplify global configuration presets`
  - 简化全局配置预设逻辑。
  - 减少“保存”和“覆盖”重复、同名配置堆积等问题。
- `a84ce05 Improve table capture and undo safety`
  - 改进表格随正文捕获。
  - 添加删除行二次确认或撤销安全相关逻辑。
- `4b14925 Prioritize table content workflow`
  - 表格页面把表格内容放到更靠前位置。
  - 表格框架 / 表格组 / 导入等配置折叠到后面。
- `a5c4e0d Fix table capture hints and dedupe`
  - 修复表格随正文解析重复读取两遍的问题。
  - 改进捕获提示。
- `2fce145 Fix table info hint popover`
  - 修复表格页面小 i 提示位置。
- `8cdd008 Avoid duplicate inline table context`
  - 避免随正文填表提示词里重复携带表格上下文。
- `07da0e0 Simplify table context controls`
  - 删除字段级“注入上下文 / 注入上限”等重复控制。
  - 让表格上下文注入改为更集中的控制。

### 2026-06-28：楼层收纳、顶部入口

- `16944ba Add persistent floor archiving and vector limits`
  - 加入持续楼层收纳 / 自动只保留最近正文楼数。
  - 向量记忆增加限制项，避免过量召回。
- `9f852d9 Fix persistent floor archive balancing`
  - 修复删除 / reroll 后自动收纳楼层不恢复的问题。
  - 让“只保留最近 N 楼”更接近隐藏助手逻辑。
- `74b4aa4 Simplify auto archive controls`
  - 楼层收纳 UI 简化为常用项。
  - 用户不喜欢“高级”字样，也不想看到太多按钮。
- `7557462 Add optional top navigation entry`
  - 扩展设置里新增开关：是否在顶部导航栏显示剧情剪辑台按钮。
  - 保留魔法棒入口，顶部入口可选，避免和主题美化冲突。

### 2026-06-29 至 2026-06-30：PC UI 尝试与 toast 修复，向量召回重做起点

- `38cb913 Polish desktop UI and localize labels`
  - 尝试优化 PC 端布局。
  - UI 文案全中文化。
  - 注意：用户后来觉得 PC 重构方向不完全合适，后续没有把它作为最终方向继续大改。
- `360c637 Fix toast background icon repetition`
  - 修复 SillyTavern 全局消息提示框背景被插件小 i 图标污染的问题。
- `1f7e52c Revamp vector recall and restore toast styling`
  - 开始重做向量召回逻辑。
  - 恢复/修复全局 toast 样式影响。

### 2026-07-01：向量索引和分支记忆清理

- `4b63619 Fix vector indexing and branch memory cleanup`
  - 修复向量索引建立 / 刷新。
  - 处理分支、删除、重 roll 后旧记忆污染问题。

### 2026-07-02：表格注入开关、阶段总结注入、向量拆分

- `0b16acd Add table memory injection toggle`
  - 表格记忆增加总注入开关。
  - 解决表格内容被多个位置重复注入的问题。
- `9a49e37 Refine vector recall recent-window skip`
  - 改进“内容仍在最近窗口内时跳过召回”的判断。
  - 后来发现要结合隐藏楼层和实际可见上下文继续修。
- `98338eb Preserve stage memory and add vector fallback`
  - 保证阶段总结进入长期记忆。
  - 向量召回增加 fallback。
- `8492e3c Fix stage injection and vector scoring`
  - 修复已有摘要模式下阶段总结没有注入的问题。
  - 调整向量评分逻辑。
- `e8fd14e Split vector body and summary indexes`
  - 向量索引拆成正文索引和摘要索引。
  - 目标是全文召回省略摘要块，摘要块单独召回。
- `b6b2303 Add vector query model fetching`
  - 查询重写模型支持拉取模型列表。
  - manifest 版本号开始持续递增。
- `6a7beba Fix vector recall window and memory records`
  - 修复向量最近窗口判断。
  - 让记忆库显示和索引记录更一致。

### 2026-07-03：向量召回详情持续打磨

- `4eb1455 Improve vector recall ordering and details`
  - 向量召回结果按楼层 / 时间顺序整理，避免剧情顺序错乱。
  - 添加更详细的召回过程展示。
- `aed9385 Improve vector recall details and saved summaries`
  - 修复记忆库中保存摘要显示不全或不显示的问题。
  - 继续改进“上次召回详情”。
- `ea2ef9f Consolidate vector recall details`
  - 把最近召回、索引片段预览等内容合并到“上次召回详情”思路里。
  - 减少重复面板。
- `6af3f36 Improve vector query detail display`
  - 查询重写详情显示优化。
  - 展示检索意图和多条线索。
- `ccda3fd Distinguish vector query rewrite display`
  - 避免向量查询重写展示太像其他插件。
  - 区分 UI 表达和文案结构。

### 2026-07-05：多次总结、移动滚动、查询重写、批量摘要

- `4fc7e63 Inject all active multi summaries`
  - 修复多次总结只注入一条的问题。
  - 改为注入所有活跃多次总结。
- `2c1247c Fix mobile navigation and preview scrolling`
  - 修复左侧菜单切换闪屏和记忆预览滑动问题。
- `943d1ac Stabilize mobile preview interactions`
  - 减少手机端摘要预览动效和滚动冲突。
- `340d1cf Fix sparse mobile preview scrolling`
  - 修复摘要数量少时，点开一个摘要后再点另一个会滑不动的问题。
- `f7fc9be Fix table edit parsing with apostrophes`
  - 表格解析支持字符串里的单引号场景。
  - 解决 `有趣` 这类单引号导致 `<tableEdit>` 识别失败的问题。
- `6e8b2b0 Improve vector query rewrite prompt`
  - 默认查询重写 prompt 改向“检索意图 + 多线索”结构。
- `a04c6e0 Harden vector query rewrite parsing`
  - 过滤 Qwen 等模型输出的规则残渣、英文说明、无效 Clue。
- `8cfcebb Improve vector rewrite feedback`
  - 查询重写 / 建索引 / 测试召回增加更明确反馈。
  - 便于用户知道按钮是否正在执行。
- `5d3a93d Polish batch summary workflow`
  - 加入批量摘要入口。
  - 区分旧正文补课和补写缺失摘要。
  - 解决 360 个缺失摘要不应变成 360 次 API 请求的问题。
- `21164ea Allow clearing stuck queue tasks`
  - 任务队列支持清理卡住的生成中任务。
  - 解决旧正文补课 14/15 等旧任务无法取消的问题。

### 2026-07-06：批量总结、真实隐藏楼层、提示词整理

- `8b27134 Add batch summary controls and real hidden count`
  - 控制台隐藏楼层数读取酒馆实际隐藏楼层。
  - 阶段总结 / 多次总结加入批量模式。
- `d53e4cd Refine batch summary controls`
  - 合并“生成阶段总结”和“批量阶段总结”入口。
  - 合并“生成多次总结”和“批量多次总结”入口。
  - 批量摘要面板排版优化。
- `5d3e9e7 Clarify missing summary titles`
  - 补写缺失摘要标题要求使用原楼层号，但允许 AI 根据正文生成短标题。
  - 避免乱写章节号。
- `995f215 Move batch sizing into batch flows`
  - 把旧正文每批楼数从自动总结设置移到批量流程弹窗。
  - 减少用户在多个页面来回找配置。

### 2026-07-07：自动总结事务和移动端提示词滚动

- `2ce4b5a Add auto summary rollback tracking`
  - 增加自动总结事务记录。
  - 记录自动总结覆盖范围、生成的总结、自动隐藏范围。
  - 主要用于将来处理 reroll / 删除楼层后的状态一致性。
  - 用户后来觉得“待确认里的自动总结回滚”容易误触，所以 UI 入口被隐藏或弱化。
- `3b58735 Fix prompt editor mobile scroll`
  - 添加本交接文档初版。
  - 修复生成提示词编辑页手机端滑不动。
  - 增加 `stabilizeMobileWorkbenchScroll(expectedTab)`。
  - 调整提示词页移动端滚动 CSS。
  - 阶段 / 多次默认提示词加入时间跨度要求。
  - 阶段总结编辑后优先使用用户手动标题。
  - 隐藏自动总结回滚入口。
  - 增加预览重渲染保护，缓解总结页只显示某一类摘要的问题。

### 2026-08-10：新手工作台第一批（本地预览，未推送）

- 控制台主卡开始读取现有聊天状态并推荐下一步：扫描、阶段总结、多次总结、待确认、任务进度或旧聊天补课设置。
- 常用操作移除重复的小字说明，只保留动作名称；动态主卡保留与当前状态直接相关的一句提示。
- 左上角菜单新增独立“帮助 / 使用说明”入口，说明集中在帮助页，不再散落到每个功能区。
- 使用说明已经从最小骨架补成四类目录、手册封面和文章阅读页；快速开始、推荐流程、功能说明与常见问题均可进入详情。
- 工作流、记忆库、自动记忆、表格、总结、摘要树、待确认、自动总结、扫描、向量、注入、提示词、主题和维护页的静态页首说明已迁入手册，原位置改为场记分隔装饰；动态运行状态仍直接展示。
- 仅调整界面结构和状态展示，不新增存储字段，也不修改摘要、配置、表格或向量数据逻辑。
- 本批完成后同步到电脑本地酒馆供人工查看；除非用户另行要求，不运行完整测试、不打开浏览器、不推送。

### 查更精确历史的方法

如果需要看最近提交：

```powershell
git -C BakemonoMemory log --oneline -20
```

如果需要看完整提交标题：

```powershell
git -C BakemonoMemory log --reverse --date=short --pretty=format:"%h %ad %s"
```

如果需要看每次提交改了哪些文件：

```powershell
git -C BakemonoMemory log --reverse --stat --date=short --pretty=format:"commit %h %ad %s"
```

## 新窗口接续步骤

如果新开 Codex 窗口继续：

1. 先让新窗口读：
   `BakemonoMemory\DEVELOPMENT_HANDOFF.md`
2. 再跑：
   `git -C BakemonoMemory status --short --branch`
3. 如果用户说“继续刚才的”，先确认最近一条用户需求，而不是从旧上下文猜。
4. 小步修改，别大重构。
5. 改完跑：
   - `node --check BakemonoMemory\index.js`
   - `git -C BakemonoMemory diff --check`
6. 如果用户要推：
   - 同步 E:\ 安装目录。
   - 检查安装目录 `node --check`。
   - commit + push。
7. 最终回复要短，说明改了什么、检查了什么、是否推送。

## 当前最值得继续观察的问题

1. 手机端 `<details>` 展开后的滚动冻结是否彻底解决。
2. Qwen/Qwen3.5-27B 查询重写是否仍输出无效英文残渣。
3. 多次总结是否全部注入，而不是只注入一条。
4. 阶段总结编辑后的标题是否始终使用用户手动标题。
5. 大批量补写缺失摘要是否真的按批生成，不再一楼一请求。
6. 批量应用摘要是否仍会导致手机酒馆卡顿；`1.1.29` 保留了隐藏工作台渲染和扫描峰值优化，需要继续用几百楼真实档观察。
7. 自动楼层收纳是否能像隐藏助手一样动态维护，而不是点一次生效一次。
8. 表格随正文解析是否仍有重复读取；新的事务级联回退要继续用真实删除 / reroll 场景验证。
9. 向量召回是否会召回当前可见正文、已注入阶段总结或 user 楼层。
10. 新首页的“已整理至 / 待识别楼层”是否符合真实长聊天的直觉，尤其要观察早期缺口与后期已整理内容并存时的显示。

## 给后续 Codex 的一句话提醒

这个插件不是普通“小工具”，它已经承担了用户长篇 RP 的记忆系统。每次改动都要优先考虑：手机端是否能用、长聊天是否会卡、token 是否重复、用户是否知道下一步该点哪里。

## 2026-08-11：最终模块化（本地检查点，未推送）

- 已先建立 `backup/pre-modularization-v1.3.5-20260811` 标签与完整 Git bundle，原发布提交 `fb5b876` 可随时恢复。
- 提示词清单、帮助手册、工作台导航/布局、操作反馈与小 i 帮助弹层已从 `index.js` 分离。
- 摘要层级、记忆记录模型、材料选择、生成目标选择和摘要任务队列已分离为独立模块。
- 表格作用域/表格组/行数据、撤销与级联回退、表格提示与操作模型、最新回合表格流程及表格工作台 UI 已按职责分离。
- 向量表单设置、索引/查询改写/混合召回引擎、向量操作控制器和向量工作台 UI 已分离；数学、Provider URL、查询解析和存储裁剪仍保持更小的底层模块。
- 自定义主题的格式校验、内置主题定义与主题编辑/预览/导入导出控制器已分离；`index.js` 不再直接持有主题编辑草稿和页签状态。
- 全局设置迁移、聊天状态补默认值/结构迁移/越界清理，以及提示词/区域/表格/随正文预设注册与当前全局配置同步已拆为独立状态服务。
- 最终注入内容、表格/向量/随正文提示组装已进入注入服务；隐藏已总结楼层、范围收纳、自动保留最近楼层与恢复操作已进入归档控制器。
- 最新助手回合识别、随正文捕获、角色卡/世界书参考与回复后摘要流程已进入回合处理控制器；扫描、回合处理、阶段自动总结、收纳与向量刷新的触发顺序已由独立记忆编排器统一决定。
- 酒馆主模型调用、自定义 OpenAI 兼容生成接口、流式响应读取与模型列表拉取已进入独立生成客户端。
- 摘要草稿创建/确认/丢弃/重生成、缺失摘要批量应用、已保存摘要编辑删除、撤回保存，以及自动总结事务失效检查与级联回滚已进入摘要草稿事务服务。
- 标签/全文片段提取、摘要类型识别、标题与纯文本读取、消息 swipe 身份、扫描结果合并已进入内容块服务；聊天遍历、扫描状态落盘和局部刷新由独立扫描控制器负责。
- 摘要手账元信息解析、分栏预览布局、摘要编辑控件和降级预览卡已进入独立预览渲染器，扫描识别不再直接持有 DOM 展示实现。
- 阶段总结/多次总结的单次与批量任务构造、材料范围确认、层级标题和三类生成提示词组装已进入摘要生成控制器；正式生成仍统一走原任务队列。
- 旧聊天补课、缺失摘要楼层识别、按批切分、批量提示词渲染、逐楼草稿拆分与批量摘要表单读取已进入摘要补写控制器。
- 扫描/自动化/API/生成提示词/回合处理/注入等设置表单读取、完整配置快照、跨角色卡共享配置持久化、首次共享升级与 JSON 预设导入标准化已进入配置服务。
- 模块装配顺序已按依赖重排；对摘要草稿、目标选择、任务队列、归档和回合处理等真正的循环位置使用惰性适配器，避免入口加载阶段访问尚未初始化的服务。
- 完整配置与扫描/自动化/API/提示词/回合/注入/向量等区域配置的应用、保存、覆盖和设为跨角色卡默认已进入配置控制器；入口只保留对应按钮事件。
- 记忆档案的状态统计、搜索/类型/状态筛选、排序、分页和记录卡片渲染已进入独立 UI 模块；档案页分页状态不再由入口文件持有。
- 首页本轮注入的来源拆分、Token 计数缓存、上一轮 Prompt 占比匹配与构成条渲染已进入独立概览模块；提示词清单与首页统计通过惰性适配器互相协作，不把循环依赖带回入口。
- 工作方式标签、楼层记忆状态索引、编排计划、首页推荐与健康判断已进入工作流概览模型；首页运行状态和当前配置清单的 DOM 更新另由概览 UI 模块负责，决策逻辑与美术呈现不再混写。
- 总结页的阶段/多次/批量页签状态、材料统计、进度与主按钮切换已进入总结生成 UI 模块；自动记忆和表格运行状态、流程步骤与表单回填已进入随正文 UI 模块，生成及表格事务逻辑保持不变。
- “自动与数据”及“设置中心”的工具开关、记录数量和当前配置摘要，以及自动总结阈值进度与运行状态，已进入独立中心概览 UI 模块；该模块只读状态，不负责保存配置。
- 总结浏览页的当前类型、独立页码、筛选排序、分页控件和摘要卡片列表渲染已进入独立浏览 UI 模块；这些仅为临时页面状态，不写入聊天配置或记忆数据。
- 生成提示词结构预览、最终注入来源概览、扫描识别统计和受移动端数量限制保护的扫描结果列表已进入页面概览模块；入口只转发当前状态和用户交互。
- 待确认草稿、保存历史、任务队列及三者页签/历史页码状态已进入审核队列 UI 模块；任务完成后回到历史第一页的既有行为通过共享状态句柄保留，摘要事务仍由原服务执行。
- 安全维护的自动总结事务、最近影响、操作足迹与 JSON 导出已进入维护 UI 模块；摘要树的覆盖关系构造、可见页节点创建和页码状态另由时间线 UI 模块负责，避免维护与记忆层级再次耦合。
- 整套配置、区域配置与自定义模型候选的下拉框 DOM 组装已进入预设控件 UI 模块；配置保存和应用仍由配置控制器负责，入口只保留事件绑定。
- 工作台页面标题、桌面/手机眉题、页面动态计数与静态“注入开/空/关”状态已进入顶栏 UI 模块；导航只通过惰性回调读取标题，搬移时同时恢复了相关中文显示文本。
- `index.js` 正在逐步收缩为启动、SillyTavern 适配和模块装配入口；此阶段不改变数据格式，不推送远端。
- 当前只执行 `node --check` 与 `git diff --check` 等源码检查；完整测试和浏览器验证留到全部拆分完成后由用户决定。
