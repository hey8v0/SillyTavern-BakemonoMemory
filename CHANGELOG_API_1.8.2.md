# v1.8.2 · 模型选择与千帆个人版

- 千帆 Token Plan 个人版加载官方模型候选，支持生成接口与查询改写接口。
- 生成、嵌入、查询改写的模型框增加完整列表入口，保留当前模型时也可直接换选，继续支持手填。
- 列表在本页会话内保留，更换接口地址或密钥时清理旧候选。
- 拉取生成模型使用表单草稿，选择后保存设置；旧请求结果不会覆盖新列表。

## 千帆配置

使用 OpenAI 兼容地址 `https://qianfan.baidubce.com/v2/tokenplan/personal` 和个人版专属密钥。生成请求保留该个人版路径。

候选取自 [千帆官方文档](https://cloud.baidu.com/doc/qianfan/s/Dmrabu8b6)，核对日期 2026-09-22。它是插件内置目录，不是账户实时模型列表，也不验证密钥；实际可用模型以套餐为准，可手填新模型。`qianfan-code-latest` 对应千帆控制台选定的模型。个人版候选用于聊天，向量索引需配置嵌入接口。

## 验收

- `tests/model-selection.test.mjs`：个人版路径与候选、普通接口错误、表单隔离、过期响应、模拟生成请求。
- `tests/offline/model-picker-ui.mjs`：三处真实设置 DOM、完整列表、选择事件、手填值、候选缓存与失效、CSS 契约。
- 离线测试不等于千帆账户实测；手机原生选择菜单仍需实机核验。
