# v1.7.0-beta.3 检查说明

代码位于 main。剧情状态改为自动收录与事后修改。

## 使用与验收

1. 先备份聊天和记忆恢复包。账本规则升为 2，兼容旧版 1；旧插件不支持新账本，回退代码时使用升级前聊天副本。
2. 默认随正文维护，不要求开启自动摘要，不增加独立请求；保留独立提取、回复后复用。独立提取会额外调用模型。
3. 模型可直接输出 state 人物、关系、约定、物品、地点和时间。省略字段保持原值，摘录不必填；旧 events 兼容。非法结构或不成立的关联组跳过，不产生审核队列。
4. 明确摘要头部时间/地点/在场人物仍可直接读取；其他叙述由模型整理。说法、观察不改变世界状态。
5. 详情“修改记录”可直接保存，时间页可修改日期及当前场景。失败保留输入并回退数据，旧表单不能覆盖新修订，历史快照只读。
6. 维护设置内编辑提示词、应用、另存、覆盖、删除预设。载入仅填编辑框，预设跨聊天共用；三种维护方式使用已应用指令，只有回读到设置才提示保存成功。
7. 旧待确认来源仍有效时自动重试一次，不新增 API 调用；过期来源留档，忽略/拒绝不复活。摘要草稿仍走原审核流程。
8. 测试 swipe A→B→A、重生成、修改正文、切聊天和重载；记录不串来源，不重复当前状态。随正文协议在阅读区隐藏，编辑和导出保留；独立模式不追加正文。
9. 人物临时状态、物品数量等应进入当前状态注入及类型化检索。说法/观察有类型标识，未采用项不进入正式状态。
10. 原摘要、阶段/多次总结、表格、Hybrid RAG、自动索引和全局配置继续回归；实机视觉由用户检查。

## 已执行检查

- 423 项 Node 回归：自动收录、旧记录迁移、直接更正、信息替代、来源隔离、摘要编辑后重新收录、保存失败、字段保留、身份引用、提示词预设及原功能；提示词接线执行入口实际回调和真实全局初始化，覆盖默认读取、保存核验、重载、设置替换及三种提取方式。显示过滤覆盖多行、流式尾部、代码围栏、用户规则保留、安装幂等和入口实际接线。
- 130 个运行模块语法解析，完整 CSS 解析。
- 14 组离线界面与存储检查；tests/offline/rp-auto-ui.mjs 覆盖各类编辑、焦点/返回、保存核验、预设 CRUD、过期表单和跨聊天保护。tests/offline/rp-display.mjs 使用只读宿主的实际格式化、正则函数与 Showdown，覆盖加粗、代码、表情、图片、旧消息、流式、重 roll、display_text、组件节点保留、原文及编辑区保护；消毒与 DOM 为离线模拟。
- 205 个合成实体、20 次离线总览渲染 179.5 ms。360/390/430px 只检查标记与 CSS 约束，不代表浏览器布局或手机实测。
- 未打开浏览器、未同步本机酒馆、未调用真实模型 API。

运行单元回归：
```powershell
node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test tests/*.test.mjs
```

离线 UI：安装测试依赖 linkedom 后运行 `node tests/offline/rp-auto-ui.mjs`；也可让 `BAKEMONO_TEST_LINKEDOM` 指向已有 linkedom 模块路径。

协议显示：同样配置 linkedom，并将 `BAKEMONO_TEST_TAVERN` 指向已安装 Showdown 的 SillyTavern 根目录，运行 `node tests/offline/rp-display.mjs`。只读取宿主源码，不启动酒馆、不写入聊天。

## 后续开发项

- 历史补录、撤回、重新提取的完整差异浏览。
- 新摘要与事实修订关联、摘要过期联动、RP 状态与旧表注入去重。
- 大账本增量重放与可丢弃检查点；当前仍完整重放。
- 更多来源/恢复边界、深层恢复结构校验及自定义历法。
- 主动新建对象界面、更多字段编辑和完整手机视觉验收。
