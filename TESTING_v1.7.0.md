# v1.7.0-alpha.6 测试说明

分支：`feature/rp-core-v1.7.0`。这是开发测试快照，不是完整 v1.7.0 的发布验收。主分支未合并；实机测试通过后仍需补齐下列开发项并重新检查。

## 开始前

先在原版本导出聊天及记忆恢复包，再用聊天副本测试。不要覆盖唯一存档。仅切换插件代码分支不会还原已写入的聊天数据；回退时使用测试前的聊天副本和备份。

## 本次可测

- 自动召回：启用向量和总注入，不点击测试直接发送消息；查看本轮查询与实际提示。重生成／swipe／续写同样触发；静默生成／代写用户不触发。超时最多等 20 秒后继续主回复，不注入旧结果。需要支持 generate_interceptor 的宿主。
- 并发：旧查询的迟到成功／失败不能覆盖新结果；切聊天、取消或更改召回参数后，旧请求不能回写。清空索引会取消在途召回。
- 模式：按楼层每楼一条，不受分档全文名额约束；按命中片段允许同楼多条并遵守每楼上限。片段、摘要和全文的类型与统计一致，重载后不把片段改称摘要。
- 长楼：命中靠后的内容时，单条和剩余预算裁剪均围绕该位置；详情显示清洗后正文位置。更新不需要清空旧嵌入。
- 保存：应用向量配置和开关后，分别显示共享配置核验与聊天保存请求结果；断网、回读不到对应修订、旧配置被新配置替代时，不能显示整套保存成功。

- 召回：重新测试一次即可使用新分档，不必重建已有嵌入。高分正文标签摘要可取回本楼正文，低分正文回退到本楼摘要；保存的总结及 RP 信息保持独立，同一起始楼不再互相挤掉。
- 结果：条数和全文数为上限；预算包含向量段来源文字，原有详情显示未采用、降级和截断原因。最终详情应与生成的向量段正文一致，主结果列表可见第五条。总注入开关关闭时仍不会发送。
- 名称：查询与记忆共同出现的连续人名或短语正常显示；只有零散字匹配时不声称命中了完整人物。内部 BM25 字片段收在详情中。近期窗口与重叠字数设为 0 后应保留实际效果。

- 正文显示：仅在聊天阅读区移除 rpEvents 的渲染内容，保留原文、swipe 与编辑框；覆盖宿主引号高亮、转义标签、代码块及流式 DOM 重绘，不重新渲染整段正文或删除其他组件。
- 重 roll：来源身份同步至当前 swipe，切回已处理分支不重复入账。当前历史、待确认及 RAG 排除旧回复来源；旧记录在“历史 → 旧回复”保留。删除前一 swipe 导致编号前移时，保留分支身份。
- 待确认校正：省略号拼接摘录可预览包含中间叙述的完整原文，不自动放宽校验；缺失对象可选择已有对象或凭证据登记人物、地点、已有物品，并原子提交关联行为。没有持有依据时放置行为仍不成立。
- 向量配置：新聊天、隐藏工作台、自动索引、切聊天后，已提交设置不被隐藏表单覆盖；拉取模型不自动保存配置或选择第一项；“应用配置”不依赖测试召回成功。
- 窗口：缩放、横竖屏和重复打开工作台时，折叠同步不再把事件对象当成 DOM 节点。
- 剧情状态：场景概览、人物／关系档案、约定日期卡、物品持有与所有权、地点和逐楼历史使用四页布局。关联详情可连续打开和逐层返回；列表保留分页与搜索，未启用和无记录时展示单一起步界面。
- 历史快照：进入后可切换四页，所选楼层保持有效；详情与来源关联不展示后来的记录，返回当前不改变数据。待确认入口继续使用同一审核数据源。
- 暖纸主题：内置日间为浅纸与灰蓝，夜间为炭灰与淡蓝；旧内置配色自动升级。另存主题、单独编辑的主题值和跟随酒馆选择保留。
- 文尾追加：向量索引有限次数跟进内容变化并复用已完成嵌入；持续变化会暂缓、等待稳定后再刷新。真实接口错误单独报告；无法自动区分无标签小组件和剧情。
- 自动提取：新启用默认随正文维护，不要求开启回复后处理或插件摘要生成；请求模型在原 bakemono 外另输出 rpEvents。旧版 reuse 设置继续兼容原流程，无对应流程时回退随正文。可明确选择回复后复用或独立请求；后者使用默认生成模型、产生额外调用。
- 首次手动处理同样遵守自动应用开关，不强制全部审核；重新提取旧楼层仍需差异审核，不能静默改写历史。
- 来源异常：显示具体原因；可粘贴原正文摘录、检查唯一定位并确认校正，再单独审核事件。校正留审计记录，来源/聊天/修订变化会使旧预览失效；不能把摘要改写直接当成原文。
- 事件操作块排除在普通正文向量、查询改写上下文与后续摘要材料之外，避免尚未确认候选以正文名义进入记忆。
- 事实、角色说法、观察分开保存；明确无冲突的事实可自动应用，其余进入共用待确认区。关联候选整组校验，确认前展示组内证据及影响数量。
- 人物、关系、约定、物品、地点和日期派生状态；历史按记录时已知内容查看；重新提取不会静默替换已确认事实。
- RP 数据进入恢复包及恢复保护；旧恢复包没有 RP Core 时不清空已有账本。恢复导入后自动提取暂停。正式三轨材料接入原 Hybrid RAG，带类型和有效性；未确认候选不注入。

## 建议实机顺序

1. 不启用 RP Core，先验证原摘要、草稿确认保存、表格和向量；保存后重开聊天，再重开酒馆。
2. 在新测试聊天开启向量，修改接口、模型和召回参数；切到另一个新聊天再返回，确认配置保留、索引和命中不串聊天。
3. 进入“剧情状态”，先空白启用。不开启回复后处理，继续使用原预设的 bakemono 摘要；生成一轮含明确人物和物品转移的正文，核对额外 rpEvents 的处理与原摘要是否都保留。没有输出事件块时不能假装已维护。另测“怀疑”“说过”和“表白未接受”，不得变成确定事实。
4. 在另一聊天副本预览旧表导入，核对字段、重名和模糊值；确认前不应产生状态。原表应保留，导入只建立当前基线，不生成过去事件。
5. 独立请求仅在需要时开启；测试停止、失败后手动重试、生成期间切聊天。观察是否出现重复事实或写入错误聊天。
6. 测试 360／390／430px 手机四页导航、搜索回车、人物→关系→约定逐层返回和待确认联动；历史快照切页后不应列出后来才入账的记录。选择两套暖纸主题，核对浅色、深色及重开后的配色；自定义主题不应被改色。
7. 保存、重载，导出恢复包并在测试副本导入，核对三轨记录及审核决定。出现异常时保留备份，并提供脱敏诊断及复现步骤；不要公开密钥或私人聊天。
8. 来源异常候选：确认按钮不可直接强行保存；用完整原句检查定位，核对楼层再校正。只有摘要里存在或多处重复的摘录应继续被拦住；可以暂不记录，不能要求用户填写 ID 或 hash。校正后再重提同楼，不应静默重复入账。
9. 同一楼层 A→B→A 切换、删除旧 swipe、重开聊天：当前说法不重复，旧候选不占当前待确认；正常阅读时无 JSON，编辑框仍有原块。旧版本来源已丢失的记录应保留为旧来源，不依据内容相似度复活。
10. 对象校正：选择现有人物，或登记有证据的人物、地点、物品；预览不写入，确认失败不能留下半组登记。改变对象选择、切聊天或修改正文后，旧预览不得生效。

## 尚未完成，不能按正式功能验收

- 完整的人工修改候选行为、可搜索的实体校正、历史补录／更正／撤回界面；失效事实的独立待处理流程。目前支持证据校正、原生选择框对象映射和有限的对象登记，不含修改任意行为或补建关系／约定。
- 重新提取的完整新增／修改／未检出／歧义列表，以及逐字段影响预览；当前仅提供已有候选审核和单目标替代。
- 新摘要与事实修订的来源关联、事实更正后的摘要过期联动；RP 状态与旧表的注入去重还需完善。
- 全部来源身份／分支／恢复边界、领域与时间规则、深层恢复包结构校验；迁移暂不自动导入旧剧情时钟。
- 其他设置页面的全局保存反馈与配置修订提升的最终审计；本次已完成向量应用／开关的回读核验。
- 大账本增量重放、可丢弃检查点、取消及合成长档性能测量；当前 RP 重放仍为完整重放，不代表手机长档性能已达标。
- 完整详细手册、全部计划验收场景及手机实机视觉检查。

## 已执行检查

- Node 回归：354 项通过，包含自动召回、并发保护、模式与预算、长楼窗口、共享配置回读及原有回归。
- 运行脚本：123 个模块语法解析通过。
- 完整样式解析；离线 DOM 检查向量草稿隔离、状态导航、共用审核、保存失败回退、基线预览与旧预览失效、搜索回车及未知版本保护。
- 离线 IndexedDB、元数据序列化重载、旧剧情工具、恢复前备份和切聊天保护检查通过。
- 新增离线 DOM 验证阅读显示清理、消息重绘、监听绑定、原文／编辑框保留，以及对象校正、保存失败回退、完整原句预览与人工确认。未运行浏览器或手机实测。

这些是离线模拟检查，不是浏览器、手机或真实 API 测试。本次未打开浏览器，未同步本机酒馆。
