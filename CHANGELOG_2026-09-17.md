# 9/9—9/17 更新汇总 · v1.7.4

## 🎭 剧情状态

- 新增独立的剧情状态系统，记录人物、关系、时间、地点、约定与物品；总览点击对应区域进入详情。
- 支持随正文维护和独立调用两种方式。模型自动收录可用记录，不再要求每楼手动确认；记录后仍可修改。
- 事实、角色说法与观察分开保存，支持查看历史；改进重 roll、重复记录与格式容错。
- 维护提示词可编辑、应用并保存为预设。随正文的 rpEvents 在正常阅读时隐藏，编辑原文仍可查看。
- 剧情状态按用户输入组织简报直接注入，不再混入向量召回，也不会被计为摘要。

## 🔍 记忆与召回

- 加入 BM25 混合检索和增量索引，未变化的片段复用已有结果。
- 接通主回复前自动召回，修复旧请求覆盖新结果、切聊天后向量配置重置等问题。
- 改进中文命中词显示、全文/片段召回和注入预算；长正文优先保留命中附近内容。
- 改善正文追加内容后的索引刷新与重试；无标签的小组件仍无法保证自动识别为非剧情。
- 正文片段、标签摘要和已存摘要分开统计，避免数量混淆。

## 📚 总结与存档

- 保留普通摘要 → 阶段总结 → 多次总结 → 继续压缩，补充可暂停、继续的总结任务队列。
- 修复多次总结覆盖、错误归档和来源有效性判断，增加旧摘要来源检查与修复预览。
- 增加可导入的记忆恢复包和脱敏诊断。恢复包含私人记忆，不含 API 密钥、完整聊天正文或向量索引。

## 🎨 界面

- 调整暖纸日间/夜间配色，重做剧情状态总览，表格与剧情状态分开。
- “本轮注入”改为统一六格，模板/规则移到前面，修正分隔线。
- **现在可直接点击每个模块查看该模块的注入预览**：分段、限高、可滚动，点击外部或关闭按钮收起。
- v1.7.4 修复手机预览弹层越界、外部点击无法关闭及底层界面被锁住的问题。
- “查看上一轮”仍保留，用于核对实际已发送内容；模块弹层展示的是当前配置预览。

更新前建议备份聊天，更新后刷新页面。旧摘要若显示“需重建”，先做来源检查；正文确实变更的内容需要从下层摘要逐级重建，不能强行恢复有效。独立剧情状态调用会额外消耗 API，随正文模式不增加独立请求。
