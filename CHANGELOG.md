# 更新日志

最新版本在最上面。每个版本的“验证记录”可以展开查看；更早的历史见 Git 提交记录。

## v1.10.0 · 剧情状态改版，外观可选日夜

剧情状态按“电影打板”的新样式重做，这是新样式的第一个页面，其他页面之后逐个跟上。

- **四个标签**：原来的总概览、全部记录、人物关系、世界状态、历史、注入预览等十个页面，收成“概览 / 档案 / 变化 / 设置”四个标签；详情和编辑页左上角统一是“‹ 返回”。
- **概览**：
  - 顶部是一块场记板，写着场景、楼层、日期和时间。
  - 下面是在场人物（一条胶片）、人物关系图、通告单（没完成的约定，剩余时间精确到小时，原来 6 小时后到期也显示“还有 1 天”）、道具表（借来的东西排在前面）和场记笔记（角色说过的话和推测）。
  - 关系图只画当前场景里的人和与他们直接相关的人，最多 6 位；人再多就自动改成列表。
- **档案**：人物、关系、约定、道具、地点、说法都在一个列表里，可以搜索，也可以按类型筛选；手动添加记录放在列表底部。
- **变化**：按楼层排成时间线，“人物移动”会写明去了哪里。没记上的变化标红并写明原因。
- **设置**：
  - 改成一行一项的列表，开关换成拨动开关。
  - 和插件其他设置页一样，改完用底部保存栏“保存”或“放弃”，不再有单独的“保存设置”按钮。
  - 会额外花钱的操作标了价签，放进“手动操作”，不再放在概览底部。
- **外观**：“外观与主题”现在有四个选项：跟随酒馆、场记板 · 夜、白板 · 日、自定义。
  - “场记板 · 夜”和“白板 · 日”替换了原来的“暖纸夜间 / 暖纸日间”。
  - 你改过或正在用的暖纸配置会保留在配置库里，没用过的副本会自动移除。
- **修复**：跟随酒馆主题时，如果酒馆主题是半透明的，插件背景也会跟着透明，后面的聊天会透出来。现在插件会自动垫一层实心底色，换酒馆主题时也会跟着更新。

<details>
<summary>验证记录</summary>

- 581 项自动化测试通过；新增关系图（最多 6 人、超出改列表、互相关系双向箭头、推测用虚线）、剩余时间、移动去向、旧预设清理、主题名称等测试。
- 在模拟宿主页面用示例剧情检查手机（375px）和电脑（1280px）宽度：四个标签、详情、编辑保存与返回、设置的保存栏（改一项显示 1 项、改回自动消失、保存与放弃）、外观四个选项切换、白板 · 日下的剧情状态。跟随酒馆时插件外框背景已为不透明色。
- 其他页面的样式规则没有改动；它们的颜色会因为背景变为不透明而略有变化。
- 离线 linkedom/css-tree 与原生浏览器检查未运行。手机酒馆尚未实测。

</details>

## v1.9.3 · 剧情状态不再成批丢记录

- 修复：同一轮回复里，只要有一条状态变化出错（比如对已经成立的约定又“接受”了一次），与它相关联的其他变化会被一起丢掉。实测一轮 27 条变化只记下了 5 条，界面却照常显示“已处理”。现在只跳过出错的那一条，以及直接依赖它的变化（比如登记失败的物品，后面的“借出”也会跳过），其余照常记下。
- 有变化没记上时，剧情状态顶部会提示“第 N 楼有 M 条变化没记上”，并写明原因，点“查看”直接跳到问题记录。
- 底部“已处理到 N 楼”改为按实际处理情况显示；回复被修改或重新生成、新内容还没处理时，不再显示成已处理。
- “重新读取最新回复”改为“读取最新回复”（这个按钮只在还没读过时出现）。

<details>
<summary>验证记录</summary>

- 576 项自动化测试通过；新增回归测试在修复前失败、修复后通过。
- 模型在事件里用同一个 `group` 明确标成一组的变化，仍然整组处理；需要人工逐条确认的来源也保持整组确认，只有模型自动维护的记录会逐条处理。
- 在模拟宿主页面用一轮 27 条变化（其中 1 条多余的“接受约定”）检查：修复前只记下 5 条，且无提示；修复后记下 26 条，顶部提示 1 条未记上，“查看”跳到问题记录。
- 手机酒馆尚未实测。

</details>

## v1.9.2 · 总结入口合并与样式表深度整理

- 总结页只保留一个“生成阶段总结／生成多次总结”按钮。点开后在同一个范围对话框里选“生成方式”：生成一条，或分批生成（大量材料分批入队）；会记住上次的选择。原先两个按钮打开的是同一个对话框。
- 样式表再精简约 540 行（12,616 → 12,074；v1.8.7 时为 13,443）：
  - 删去 318 条在任何页面、任何宽度下都不会生效的声明，以及因此变空的 31 条规则。
  - 删去一整段永远不会生效的 `@media (max-width: 0px)` 规则（早期被停用的手机字号副本）。

<details>
<summary>验证记录</summary>

- 575 项自动化测试通过。
- 生效判断：在模拟宿主页面中打开全部页面与折叠栏，于 9 个宽度（360、415、480、580、660、710、800、1000、1280px，覆盖样式表所有断点区间）逐个元素计算层叠结果。只有在被“无条件”规则（不依赖悬停、展开、选中、运行时切换的类名或属性）始终覆盖时才判定为不生效；作用于脚本生成内容的规则一律保留。
- 结果核对：1280、800、375px 下全部页面所有元素的位置与 40 余项计算样式与改动前完全一致（0 处差异）；并截图检查了总结列表、记忆库列表和剧情状态。
- 离线 linkedom/css-tree 与原生浏览器检查未运行。手机酒馆尚未实测。

</details>

## v1.9.1 · 界面整理

- 每页不再重复标题：顶部栏已显示页面名称，页内的重复标题块（含装饰分隔线）移除，首屏多出约一个标题的高度。帮助页保留版本说明；提示词清单的条目数移入上方统计卡片。
- 去掉装饰性元素：卡片角上的斜纹胶带、概览的“SC. 00 / TK. 00”、“自动与数据”的“A”票号、设置中心的 01–09 编号；“MEMORY STATUS”“START HERE”改为中文。
- 主按钮统一为主题强调色：“处理最新正文”“建立／刷新索引”不再是米白色。
- 菜单展开时左上角改为“收起”箭头，不再与右上角关闭按钮同为 ✕。
- “返回……”链接在手机上不再是悬浮的深色标签，与电脑端一致为文字链接。
- “记忆数据库”折叠栏改用与其他折叠栏一致的样式；“自动记忆”页的摘要方式显示改为左对齐。
- 样式表去掉约 800 行：同一选择器下必然被覆盖的声明、只作用于已移除元素的规则，以及顶栏状态标签分散在 6 处的规则（合并为 1 条基础规则加各断点 1 条）。

<details>
<summary>验证记录</summary>

- 574 项自动化测试通过；新增 tests/visual-cleanup.test.mjs。
- 样式去重在模拟宿主页面中逐页比较了全部元素的位置与 40 余项计算样式：1280px、800px、375px 下改动前后完全一致（0 处差异）；顶栏标签合并同样为 0 处差异。其余外观改动逐页截图检查。
- 离线 linkedom/css-tree 与原生浏览器检查未运行。手机酒馆尚未实测。

</details>

## v1.9.0 · 摘要方式向导

原来“工作流（已有摘要／补课旧聊天／自定义）”“自动记忆 → 摘要来源”和几项工作流细节分散在不同页面，彼此牵连却可以各选各的。本版合并为一个问题。

- “设置中心 → 摘要方式”只问一个问题：新回复的摘要从哪里来？
  - 回复里已经自带摘要（角色卡／预设负责写，不额外调用）
  - 让模型在回复里顺带写摘要（不额外调用）
  - 每次回复后另外生成摘要（额外调用）
  - 需要时再手动生成
- 选择后会自动填好配套的工作流、记忆策略、阶段材料与输出风格；页面说明摘要从哪里读、是否注入。需要特殊搭配时在同页“高级：分别调整”修改，与默认搭配不同时会提示。
- 修复一个易踩的组合：摘要来源选“独立生成”但工作流仍是“已有摘要”时，插件保存的摘要不会注入上下文。现在会明确提示，并可一键“恢复默认搭配”。
- “去补写旧聊天”直接打开总结页的补写，并按所选方式预选处理方式：摘要写在回复里时预选“补写缺失摘要”，存在插件里时预选“旧正文补课”。
- 修复注入开关、插入深度与消息角色：它们此前放在工作流页却只能随注入页保存，工作流页还显示为“关闭／空白”，修改不会生效。现移到“注入内容”页，由该页保存栏保存。
- 摘要方式页与其他设置页一样使用顶部保存栏，离开或关闭时会提醒；一次选择算一项修改。原来三张工作流卡片只保存到当前聊天，现在统一保存为全局配置。
- “自动记忆”页改为显示当前摘要方式并提供“更改”入口；“自动总结”页的“摘要来源”按钮改为前往摘要方式。
- 概览、设置中心与状态说明改用新的说法（摘要写在回复里／摘要存在插件里）；帮助页与 README 同步更新。旧版组合设置保持可用，页面会提示并可替换。

<details>
<summary>验证记录</summary>

- 569 项自动化测试通过；新增 tests/summary-source-wizard.test.mjs。
- 模拟宿主页面（非真实酒馆）检查 800px、375px：四种选择与说明、配套字段、单项修改计数、撤销、离开提醒、补写入口预选、旧版组合与自定义提示、不会注入组合的修正、注入页开关与深度保存。
- tests/offline/automation-recovery-ui.mjs 已按新流程更新，但本机未安装 linkedom，本次未运行；原生浏览器检查未运行。手机酒馆尚未实测。

</details>

## v1.8.9 · 去重与说法统一

- 设置页只保留顶部“保存设置”一个保存入口，移除各页重复的“应用规则／应用自动设置／应用模型设置／应用提示词／应用编辑／应用配置”按钮（这些按钮此前已由保存栏接管，行为不变）。
- 总结页：去掉点“生成”后再选“单次／批量”的弹窗，改为“生成一条阶段总结”与“分批生成”两个按钮；原“批量生成”标签改名为“补写旧聊天”，避免与分批生成混淆。没有材料时按钮置灰，并提示从哪里开始。
- 注入页“记忆正文”改为只读并说明来源。它每次都由已保存的摘要、表格和剧情状态重新组成，此前的手动编辑与“清空记忆正文”实际不会生效，已移除该按钮。
- 记忆库：数量并入“全部／已选入／已归档”筛选按钮，去掉重复的统计行；记忆数据库说明行不再重复各类数量。
- 顶栏注入状态改为“注入已关／无可注入／注入 N 字”；手机顶栏可完整显示。
- 概览与“自动与数据”统一为“尚无摘要”的说法，与剧情状态区分。
- 剧情状态按钮按实际作用命名：“重新读取最新回复”（不调用模型）、“用模型重新提取”、“立即提取最新回复”。

<details>
<summary>验证记录</summary>

- 564 项自动化测试通过；新增 tests/interface-dedupe.test.mjs。
- 模拟宿主页面（非真实酒馆）检查 800px、375px：总结卡片按钮与空状态、记忆库筛选、注入页只读正文与模板草稿、自动总结卡片、顶栏状态、剧情状态按钮。
- 离线 linkedom/css-tree 与原生浏览器检查本次未运行。手机酒馆尚未实测。

</details>

## v1.8.8 · 切页定位与未保存提醒

- 切换页面时从新页面顶部开始，不再停在上一页的滚动位置。
- 设置页有未保存修改时，切页或关闭剪辑台会询问：保存并继续、放弃修改或留在本页。保存失败或数值有误时留在本页并定位到字段；Esc 与点击弹窗外等同留在本页。保存栏显示未保存的修改项数。
- 修复“自动总结”页出现一个空白按钮；工作台内被隐藏的元素不再被按钮样式显示出来。
- 修复“生成阶段总结 / 多次总结”的方式选择框出现横向滚动条。
- 剧情状态“从这一轮开始”的确认改为在原位置显示并聚焦“确认空白启用”，按钮间距恢复正常。

<details>
<summary>验证记录</summary>

- 557 项自动化测试通过；新增 tests/workbench-leave-guard.test.mjs 的 10 项在旧实现上全部失败。
- 模拟宿主页面（非真实酒馆）中检查 1280px、800px、375px：切页滚动、提醒弹窗三种选择与 Esc、数值错误留页、表格与自动记忆之间切换不提醒。
- 离线 linkedom/css-tree 检查与原生浏览器检查本次未运行（本机未安装对应依赖）。手机酒馆尚未实测。

</details>

## v1.8.7 · 摘要跳转与设置保存

- 修复点击“查看异常摘要”后页面空白，现可进入总结页并展开对应记录。
- 修复自动总结提醒中的“前往总结”“查看摘要”跳转。
- 修复向量默认参数被数值校验拦截的问题，楼数与字数参数支持范围内的整数。
- 设置中的数值填写错误时，显示具体字段和原因，自动展开并定位到该字段，保留未保存的输入。

<details>
<summary>验证记录</summary>

- 547 项自动化测试、13 项离线检查通过。
- Chrome 153 独立浏览器检查：1280px、390px 下使用实际模板、默认配置和设置模块，验证默认保存、整数/小数/零值、超限与格式错误、展开定位和草稿保留。
- 两项新增回归均先在旧实现复现失败，再通过修复：真实页面跳转，以及默认 1200、900、1800 的原生数值校验。
- 手机酒馆、iOS Safari 与宿主持久保存尚未实测。第 15 楼来源变化的具体原因仍需对应异常摘要和来源材料核对。

原生浏览器检查使用已有依赖，不自动下载浏览器：

```powershell
$env:BAKEMONO_TEST_PLAYWRIGHT = 'file:///绝对路径/playwright/index.mjs'
$env:BAKEMONO_TEST_BROWSER_PATH = 'C:/浏览器路径/chrome.exe'
node tests/browser/settings-save-native.mjs
```

未指定环境变量时，使用当前 Node 可解析的 Playwright 包及其已有 Chromium。

</details>

## v1.8.6 · 自动总结与补写修复

- 修复早期异常摘要阻塞当前自动总结的问题。本批材料完整时可继续整理，批次内缺少摘要仍会提示具体楼层。
- 补写缺失摘要会识别插件内已保存的有效摘要与总结，避免把没有正文标签的已有记忆再次列为缺失。
- 删楼后按来源的当前位置计算覆盖范围，并隐藏实际已总结的楼层。
- 自动总结页列出异常摘要，点击“查看摘要”可直接定位并展开对应记录。

验证：547 项自动化测试、13 项离线检查，以及运行模块语法、扩展清单与改动范围检查。手机酒馆与真实模型接口尚未实测。

## v1.8.5 · 自动处理与错误反馈

- 摘要来源集中到自动记忆页上方，可选择读取已有摘要、随正文生成、独立自动生成或仅手动生成。
- 选择独立自动摘要后，同时开启自动执行。旧的手动设置与自定义提示词保留。
- 自动总结显示排队、生成、失败、暂停、待保存等实际状态，并提供检查或重试入口。
- 修复正文对象被替换、排除内容变化时，摘要误报“正文来源已变化”的问题。
- 摘要计数与执行共用材料校验，只有标题或思考占位的内容会提示具体楼层。
- 剧情事件支持 JSON 代码块包装；格式错误会保留已有记录，同一失败输出只提醒一次，修正后可以重新读取。

验证：538 项自动化测试通过；离线页面交互、模块语法和扩展清单检查。手机酒馆与真实模型接口待实测。

## v1.8.4 · 自动总结修复

- 修复“仅提醒”切换为自动生成后，同一批材料不再触发的问题；材料检查未通过时，修正后可以重新触发。
- 修复直接读取正文、自动回退到正文时，材料已达到条件却不启动总结的问题。
- 统一总结页与自动总结页的待整理计数，显示当前材料来源、已收录数量，以及因来源设置未纳入的条数。
- 摘要保存、扫描与任务更新后及时刷新进度，保留尚未保存的设置输入。
- 修复“仅插件已保存摘要”没有可用材料时的报错，提示切换材料来源。

同一批材料已有任务或待保存草稿时，不重复生成；失败任务仍从任务队列重试。

## v1.8.3 · 填表容错与错误提示

- 修复“11月14日”等缺年份日期导致整批填表无法应用的问题，自动保留为时间描述。
- 兼容中文完整日期、斜杠日期、单引号、未加引号的字段键及末尾逗号。
- 完整检查多段填表指令，格式错误和输出截断不再被跳过。
- 草稿直接显示出错指令、表号、行列及修正提示；修正后可重新解析并应用。
- 随正文和独立填表失败时保留草稿；只读表、无效日期及越界修改继续整批拦截。
- 填表指导补充缺年份时间的写法。

<details>
<summary>验收记录</summary>

- `tests/table-tolerance.test.mjs`：日期规范化、格式兼容、事务保护、多区块与截断、自动填表失败恢复。
- `tests/offline/table-draft-feedback-ui.mjs`：草稿错误展示、修正、重新解析、应用及文本转义。
- 离线测试未使用真实模型；手机布局和宿主保存仍需实机确认。

</details>

## v1.8.2 · 模型选择与千帆个人版

- 千帆 Token Plan 个人版加载官方模型候选，支持生成接口与查询改写接口。
- 生成、嵌入、查询改写的模型框增加完整列表入口，保留当前模型时也可直接换选，继续支持手填。
- 列表在本页会话内保留，更换接口地址或密钥时清理旧候选。
- 拉取生成模型使用表单草稿，选择后保存设置；旧请求结果不会覆盖新列表。

### 千帆配置

使用 OpenAI 兼容地址 `https://qianfan.baidubce.com/v2/tokenplan/personal` 和个人版专属密钥。生成请求保留该个人版路径。

候选取自 [千帆官方文档](https://cloud.baidu.com/doc/qianfan/s/Dmrabu8b6)，核对日期 2026-09-22。它是插件内置目录，不是账户实时模型列表，也不验证密钥；实际可用模型以套餐为准，可手填新模型。`qianfan-code-latest` 对应千帆控制台选定的模型。个人版候选用于聊天，向量索引需配置嵌入接口。

<details>
<summary>验收记录</summary>

- `tests/model-selection.test.mjs`：个人版路径与候选、普通接口错误、表单隔离、过期响应、模拟生成请求。
- `tests/offline/model-picker-ui.mjs`：三处真实设置 DOM、完整列表、选择事件、手填值、候选缓存与失效、CSS 契约。
- 离线测试不等于千帆账户实测；手机原生选择菜单仍需实机核验。

</details>

## v1.8.1 · 剧情状态列表

- 重排剧情状态「全部记录」：人物、关系、角色说法、观察、物品、约定和地点分组展示。
- 人物列表显示头像和当前状态，支持搜索状态内容。
- 搜索框移到顶部，分类筛选改为下划线样式，窄屏可横向滑动。
- 调整列表左对齐、分隔线、长文本省略和小标签排版。
- 修复进入「全部记录」时沿用上一页滚动位置的问题。
- 维护提示收纳到列表底部，展开即可查看。

## v1.8.0 · 剧情状态精简与总览

- 续写读取相关现状，不再附带完整状态 JSON、近期事件账本和重复长 ID。维护使用稳定短引用，内部存档身份、历史及来源不变。
- 默认提示词精简，优先记录时间地点、在场者、持续状态、关系、重要物品和未完成约定。随正文、独立提取均保留；未修改的旧默认自动升级，自定义文本和预设不覆盖。
- 角色说法、观察与事实分开显示和注入，可记录、修改明确知情者；不把声称康复当成真实康复。
- 剧情页采用紧凑总览：时间、地点、人物直接进详情，人物行展示当前状态；全部记录支持搜索、筛选、分页。沿用正式版日/夜配色。
- 状态注入预览在面板内打开，现状与维护指令分开，长内容限高滚动。计数为字符，不冒充实际模型 Token。
- 独立提取优先保留本轮正文和状态，旧对话及设定参考按剩余预算加入。

<details>
<summary>检查记录</summary>

全量回归 493 项通过：`node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test tests/*.test.mjs`。143 个运行时模块语法解析通过。

新增 `tests/rp-compact-context.test.mjs`：长 ID 隔离、短引用稳定/解码、无效引用拒绝、姓名混用、说法分层、重 roll、手动修正、重载与独立提取预算。预设升级测试在 `rp-prompt-library.test.mjs`。

离线界面：`tests/offline/rp-compact-ui.mjs`、`rp-auto-ui.mjs`、`rp-independent-ui.mjs`；依赖路径通过 `BAKEMONO_TEST_LINKEDOM` 设置，沿用基础测试说明。不需要给插件新增依赖。

未同步本机酒馆、未调用真实模型、未完成手机像素级实测。固定输出验证数据流，不保证各模型的语义准确率；历史未记录的信息不会自动凭空补齐。建议先备份再更新，重点试一轮记录、重 roll、手动修改和手机返回操作。

</details>

## v1.7.5 · 运行稳定性与保存反馈

### 修复与调整

- 修复单独填表/自动填表传错状态参数引起的 `includeUserMessage` 报错；同一正文自动处理失败后不再反复重跑，可手动重试，新正文可继续处理。
- 向量自动索引失败后暂停，保留原索引并给出恢复入口；合并并发触发，避免同一次失败刷出多条警告。错误状态只属于当前聊天。
- 标签摘要先按索引长度限制再送入嵌入接口，索引文本与嵌入输入保持一致。更新索引签名，未变更文本的向量仍可复用。
- 嵌入测试明确区分输入框草稿与已保存配置，并说明短文本连接测试不等于长文索引完成。
- 本轮注入、模块预览和发送组装统一读取当前有效来源，不再依赖过期的 generatedMemory 缓存。保留摘要来源/覆盖校验，不强行放行失效总结。
- 区分“已选入记忆”“本轮已组装”“上一轮已核对”。空内容不能再误判为已在上一轮核实；内容为空时提供原因和检查方向。
- 向量、扫描、自动总结、生成接口、提示词、注入、摘要/表格设置增加顶部保存栏。切页暂存输入，保存未确认时保留草稿；只保存当前设置区域，不读取其他隐藏设置页。
- 保存同时等待共享配置回读核验和聊天保存请求完成；预设按钮明确标为“另存为预设”。总结页增加自动总结规则说明与设置快捷入口。

向量总开关、表格注入开关和确认后的表格组切换保留原有即时操作；它们不被误标为未保存草稿。应用预设/恢复默认仍按各自确认流程执行。

### 升级后

先备份聊天并更新插件、刷新页面。遇到索引暂停，检查并保存接口配置，再点击“建立 / 刷新索引”；不必删除摘要或聊天。

“本轮已组装”表示当前配置下准备注入的内容，实际发送仍以“查看上一轮”为准。本次没有更改默认深度或绕过来源有效性检查。

<details>
<summary>验收与边界记录</summary>

- Node 全量 485 项通过；140 个运行模块语法与完整 CSS 解析通过。回归命令：`node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test tests/*.test.mjs`。
- 新增回归：`tests/runtime-feedback.test.mjs`、长摘要输入限制与索引错误的跨聊天隔离用例。
- 离线界面：`tests/offline/settings-save-ui.mjs`、`vector-overview-ui.mjs`、`injection-preview-ui.mjs`、`summary-coverage-ui.mjs`；依赖与环境变量沿用 TESTING_v1.7.0.md。
- 离线证明的是参数错误、重复告警、缓存不一致与保存状态等可复现路径，不证明截图用户的实际接口响应或上一轮请求内容。服务商超时仍可能发生；本次未调用真实模型、同步本机酒馆或在手机实测。

</details>

## v1.7.2 – v1.7.4 · 9/9—9/17 更新汇总

### 🎭 剧情状态

- 新增独立的剧情状态系统，记录人物、关系、时间、地点、约定与物品；总览点击对应区域进入详情。
- 支持随正文维护和独立调用两种方式。模型自动收录可用记录，不再要求每楼手动确认；记录后仍可修改。
- 事实、角色说法与观察分开保存，支持查看历史；改进重 roll、重复记录与格式容错。
- 维护提示词可编辑、应用并保存为预设。随正文的 rpEvents 在正常阅读时隐藏，编辑原文仍可查看。
- 剧情状态按用户输入组织简报直接注入，不再混入向量召回，也不会被计为摘要。

### 🔍 记忆与召回

- 加入 BM25 混合检索和增量索引，未变化的片段复用已有结果。
- 接通主回复前自动召回，修复旧请求覆盖新结果、切聊天后向量配置重置等问题。
- 改进中文命中词显示、全文/片段召回和注入预算；长正文优先保留命中附近内容。
- 改善正文追加内容后的索引刷新与重试；无标签的小组件仍无法保证自动识别为非剧情。
- 正文片段、标签摘要和已存摘要分开统计，避免数量混淆。

### 📚 总结与存档

- 保留普通摘要 → 阶段总结 → 多次总结 → 继续压缩，补充可暂停、继续的总结任务队列。
- 修复多次总结覆盖、错误归档和来源有效性判断，增加旧摘要来源检查与修复预览。
- 增加可导入的记忆恢复包和脱敏诊断。恢复包含私人记忆，不含 API 密钥、完整聊天正文或向量索引。

### 🎨 界面

- 调整暖纸日间/夜间配色，重做剧情状态总览，表格与剧情状态分开。
- “本轮注入”改为统一六格，模板/规则移到前面，修正分隔线。
- **现在可直接点击每个模块查看该模块的注入预览**：分段、限高、可滚动，点击外部或关闭按钮收起。
- v1.7.4 修复手机预览弹层越界、外部点击无法关闭及底层界面被锁住的问题。
- “查看上一轮”仍保留，用于核对实际已发送内容；模块弹层展示的是当前配置预览。

更新前建议备份聊天，更新后刷新页面。旧摘要若显示“需重建”，先做来源检查；正文确实变更的内容需要从下层摘要逐级重建，不能强行恢复有效。独立剧情状态调用会额外消耗 API，随正文模式不增加独立请求。

## v1.7.1 · 摘要来源与覆盖

### 修复范围

- 选材、队列、保存、注入、扫描计数、记忆库和摘要树共用有效来源图。失效上层释放有效下层；正常覆盖、需重建、未启用注入分别显示。
- 新保存的记录使用独立身份与内容指纹，冻结正文/标签/已保存摘要的实际输入。生成前、返回后、保存前核对；输入变化不重绑、不自动重试收费，返回文字保留为草稿。
- 仅修改标签外未消费的内容不使精确标签输入失效；实际输入、删除和回复变体变化仍受保护。旧绑定不假定当年只读取标签。
- 摘要写操作串行；保存失败撤回本次字段与来源变动，不覆盖等待期间新增的草稿。恢复包 3 保留输入快照，仍支持 1/2。
- 未改 RP 规则、模型接口或本机酒馆文件。

### 旧档操作

1. 升级前备份聊天。记录页 → 摘要来源检查 → 预览来源修复。
2. 可唯一核实的旧绑定可以迁移，应用前下载记忆恢复包。真实来源版本不匹配、缺失、同 hash 身份冲突或循环不会被强行激活。只检查/预览不调用模型。
3. 看“需重建”的最底层原因，先补课或重新生成普通摘要，再阶段总结，最后多次总结。原文确实变更不能仅靠重做最上层或改标题修复。
4. 生成范围里的“包括已覆盖材料”用于主动重整；“只使用有效材料”明确排除失效部分，覆盖范围只取实际输入。默认仍会拦住坏材料，不默默跳过。
5. 来源报告只导出标识、类型、楼层、修订、引用边与原因码；分享前仍检查隐私。恢复包含私人摘要，勿当诊断报告公开。

<details>
<summary>验收记录</summary>

基线 447 项，新增 20 项实际摘要链路回归。测试调用实际选材、生成控制器、队列、草稿服务、紧凑序列化、注入组装、楼层计数和备份；模型响应、宿主磁盘写入用可控替身。

```powershell
node --test tests/*.test.mjs
node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON tests/offline/summary-coverage-ui.mjs
```

离线 DOM 使用已有 linkedom，运行时设置 `BAKEMONO_TEST_LINKEDOM` 为其 `esm/index.js` 的 file URL，不需要在酒馆安装依赖。

本次全量 467/467 通过；137 个运行模块语法、样式表解析、离线摘要界面与差异检查通过。

覆盖：四条独立上层并存；159/160/161/200/320/400 楼；过期材料阻止 API；递归压缩回退；相同输出独立身份；标签外附注、隐藏、追加、删楼、换回复；生成中修改与切聊天；保存失败及较新草稿保留；真实紧凑序列化重载；恢复包；循环/旧身份冲突；只读修复预览与版本校验。离线 DOM 检查标签与原因、文本安全、修复前备份、诊断和循环渲染。

合成 1000 条普通摘要、1001 楼，单次离线测量：来源刷新与记录组装 588.8 ms，注入材料组装 629.1 ms，1000 条保持有效。未模拟模型 token 上限；这是电脑上的合成测量，不是手机指标。

未执行真实浏览器、手机、模型调用或反馈者故障存档测试。离线通过不等于所有宿主环境已实测；没有证据支持任意加入 160 楼上限。

</details>
