# v1.8.3 · 填表容错与错误提示

- 修复“11月14日”等缺年份日期导致整批填表无法应用的问题，自动保留为时间描述。
- 兼容中文完整日期、斜杠日期、单引号、未加引号的字段键及末尾逗号。
- 完整检查多段填表指令，格式错误和输出截断不再被跳过。
- 草稿直接显示出错指令、表号、行列及修正提示；修正后可重新解析并应用。
- 随正文和独立填表失败时保留草稿；只读表、无效日期及越界修改继续整批拦截。
- 填表指导补充缺年份时间的写法。

## 验收

- `tests/table-tolerance.test.mjs`：日期规范化、格式兼容、事务保护、多区块与截断、自动填表失败恢复。
- `tests/offline/table-draft-feedback-ui.mjs`：草稿错误展示、修正、重新解析、应用及文本转义。
- 离线测试未使用真实模型；手机布局和宿主保存仍需实机确认。
