# v1.9.2 · 总结入口合并与样式表深度整理

- 总结页只保留一个“生成阶段总结／生成多次总结”按钮。点开后在同一个范围对话框里选“生成方式”：生成一条，或分批生成（大量材料分批入队）；会记住上次的选择。原先两个按钮打开的是同一个对话框。
- 样式表再精简约 540 行（12,616 → 12,074；v1.8.7 时为 13,443）：
  - 删去 318 条在任何页面、任何宽度下都不会生效的声明，以及因此变空的 31 条规则。
  - 删去一整段永远不会生效的 `@media (max-width: 0px)` 规则（早期被停用的手机字号副本）。

## 验证

- 575 项自动化测试通过。
- 生效判断：在模拟宿主页面中打开全部页面与折叠栏，于 9 个宽度（360、415、480、580、660、710、800、1000、1280px，覆盖样式表所有断点区间）逐个元素计算层叠结果。只有在被“无条件”规则（不依赖悬停、展开、选中、运行时切换的类名或属性）始终覆盖时才判定为不生效；作用于脚本生成内容的规则一律保留。
- 结果核对：1280、800、375px 下全部页面所有元素的位置与 40 余项计算样式与改动前完全一致（0 处差异）；并截图检查了总结列表、记忆库列表和剧情状态。
- 离线 linkedom/css-tree 与原生浏览器检查未运行。手机酒馆尚未实测。
